# 2025年春期 応用情報技術者試験 午後 問3（選択）
## プログラミング：スライドパズルを解くプログラム（幅優先探索）

---

## 問題文

**問3** スライドパズルを解くプログラムに関する次の記述を読んで、設問に答えよ。

表1のルールで定義されるスライドパズルについて考える。

### 表1 スライドパズルのルール

![表1 スライドパズルのルール](../images/2025春_問03_表01_スライドパズルのルール.png)

> | 用語 | 説明 |
> |---|---|
> | 盤面 | 正方形の盤に、1から順に1ずつ大きな数字の書かれた駒が配置されている。1ヵ所だけ駒が置かれていない空白のマス（以下、空白マスという）がある。 |
> | 開始時点の盤面 | 開始時点は駒と空白マスはランダムに配置されている。 |
> | ゴールの盤面 | 盤面左上に数字の1が配置され、右方向の数字と順に1ずつ大きくなる。その行の右端の数字の次には、1つ下の左端の数字が続く。盤面右下が空白のマスである。 |
> | 駒の移動 | 駒は上下左右のいずれか一方向に空白マスに移動することができ、駒が移動すると空白マスは駒がいたマスに移る。駒の移動を1手とする。ゴールの盤面と同じになるまで駒を移動し続ける。 |

一辺が3マスのスライドパズルの例を図1に示す。

### 図1 一辺が3マスのスライドパズルの例

![図1 パズルの例](../images/2025春_問03_図01_パズルの例.png)

> ※ 開始時点の盤面（例）:
> ```
> 8 1 3
> 4 _ 2
> 7 6 5
> ```
> ゴールの盤面:
> ```
> 1 2 3
> 4 5 6
> 7 8 _
> ```

本問では、一辺のマスの個数が任意のスライドパズルにおいて、ゴールの盤面になるまでの駒の移動回数が最小となる移動方法（以下、最小解という）を一つ求めるプログラムを作成する。

---

### 〔一辺がNマスのスライドパズルの最小解を幅優先探索を用いて求める方法〕

幅優先探索を行ったとき、スライドパズルの盤面の遷移を、グラフで表現する。開始時点の盤面をルートノード、ある時点の盤面をノード、駒の移動に伴う盤面の遷移をエッジで表現する。また、ゴールの盤面をゴールノードとして定義する。

幅優先探索で最小解を求める方法を次のように考える。ここで、N は2以上とする。探索対象のノードに対して、移動できる駒ごとにその駒を移動後の状態を子ノードとして作成して、探索対象のノードは探索済みとなる。また、子ノードが表す盤面が探索済みノード（以下、探索済みノードという）と同じであれば、この子ノードは終端ノードとして、以降の探索は行わない。また、子ノードがゴールノードと同じであれば、最小解が見つかったと判定して探索を終了する。

### 〔N が3の場合の例〕

一辺が3マスのスライドパズルの最小解を求める過程の例を図2に示す。

### 図2 一辺が3マスのスライドパズルの最小解を求める過程の例

![図2 BFS過程](../images/2025春_問03_図02_BFS過程.png)

> ※ 図中の × は終端ノード（探索済みノードまたはゴールノード判定で終端化されたノード）を表す。
> ※ 1回目（第1イテレーション）: ルートノードから①②③の3子ノードを作成
> ※ 2回目: ①から④⑤⑥の3子ノードを作成（②③は探索済みにより終端）
> ※ n回目: ゴールノードに到達

(1) 1回目の駒の移動では、ルートノードを探索対象のノードとする。ここでは、移動できる駒の数が3つあるので、ルートノードから3つの子ノード ①〜③ のノードを作成し、ルートノードは探索済みノードとなる。ここで ①〜③の子ノードに対して、ゴールノードの判定及び終端ノードの判定を行う。

(2) 次に、作成した子ノードで終端ノード以外のノードをそれぞれ探索対象のノードとし、駒の移動によって子ノードを作成する。図2では、①から ④〜⑥ の子ノードを作成し、①〜③は探索済みノードとなる。ここで ④〜⑥の子ノードに対して、ゴールノードの判定及び終端ノードの判定を行う。図2では、盤面が探索済みノードと一致する③、⑤及び⑥は終端ノードとなり、以降の探索は行わない。

(3) 以降、(2)で作成したノードの子ノードの作成と、ゴールノードの判定及び終端ノードの判定を繰り返す。図2の⑧は、n回目の移動でゴールノードに到達したことを示している。なお、全てのリーフノードが終端ノードと判定された場合、ゴールの盤面に至る移動方法がないことを意味しているので、所定のメッセージを出力して探索を終了する。

---

次に、配列を用いた盤面の表現方法を図3に示す。ここで、空白マスを値は最も大きな駒の数字であるNに1を加えた値とする。なお、配列の要素番号は1から始まるものとする。

### 図3 配列を用いた盤面の表現方法

![図3 配列表現](../images/2025春_問03_図03_配列表現.png)

> ※ N=3の例:
> ```
> 盤面:
> 0 6 7    → 1行目配列: {0, 6, 7}  (0は空白=9ではなく仮の値)
> 2 5 4    → 2行目配列: {2, 5, 4}
> 8 3 1    → 3行目配列: {8, 3, 1}
> ```
> board配列: board = {(0,6,7), (2,5,4), (8,3,1)}
> ※ 空白マスの位置を示すspace配列: space = {行番号, 列番号}

(1) その行ごとに定義した配列を整数型配列の配列（以下、盤面配列という）で表す。  
(2) 1行目の列番号の数字を整数型配列の配列として参照し、board[i][j]として参照する。

---

### 〔一辺がNマスのスライドパズルの最小解を求めるプログラム〕

一辺が N マスのスライドパズルにおいて、開始時点の盤面からゴールの盤面及び駒の移動順数を出力するプログラムを作成する。開始時点の盤面からの遷移を保持する単方向連結リストの要素となるクラス BoardState の説明を表4に示す。キューを実現するクラス Queue の説明を図5に、リストを実現するクラス List の説明を図6に、プログラムで使用する主な関数を表2に、最小解を求めるプログラムを図7に示す。

### 表2〜表6 クラスおよび関数の説明

![表2 クラス説明](../images/2025春_問03_表02_クラス説明.png)

> **クラス BoardState のメンバ変数:**
> | メンバ変数 | 型 | 説明 |
> |---|---|---|
> | board | 整数型配列の配列 | 盤面配列。初期状態は未定義。 |
> | space | 整数型配列 | 空白マスの場所を示す行番号と列番号の二つの配列からなる配列。初期状態は未定義。 |
> | prev | BoardState | 直前の BoardState のインスタンスへの参照。開始時点の盤面の起点からゴールまでの遷移を辿る際に用いる。初期状態は未定義。 |

### 表5 プログラムで使用する主な関数

![表5 主な関数](../images/2025春_問03_表05_主な関数.png)

> | 名前 | 戻り値 | 説明 |
> |---|---|---|
> | createGoal(整数型: board_size) | 整数型配列の配列 | 引数 board_size の値の盤面をゴールとしたスライドパズルのゴールの盤面を求めて返す。（以下、ゴールの盤面配列という） |
> | createStart(整数型: board_size) | 整数型配列の配列 | 引数 board_size の値の盤面をゴールとした開始時点の盤面をランダムに作成して返す。 |
> | getSpace(整数型配列の配列: b) | 整数型配列 | 盤面配列 b を用いて、空白マスの場所を行番号と列番号で返す。 |
> | checkGoal(整数型配列の配列: b, 整数型配列の配列: goal) | 論理型 | 盤面配列 b がゴールの盤面配列と同じかどうかを判定して結果を返す。同じ場合 true を返す。 |
> | checkSameBoard(整数型配列の配列: b, List: l) | 論理型 | 引数で渡される探索済みの盤面配列のリストに引数で渡される盤面配列が含まれているかどうかを確認する。含まれている場合 true を返す、含まれていない場合 false を返す。 |

### 図7 最小解を求めるプログラム

![図7 プログラム](../images/2025春_問03_図07_プログラム.png)

> ```
>  1: solvePuzzle(整数型: board_size)
>  2:   BoardState: start_state  /* 開始時点の盤面を保持するBoardStateのインスタンスへの参照 */
>  3:   整数型配列の配列: goal_board  /* ゴールの盤面配列への参照 */
>  4:   整数型配列の配列: direction  /* 駒の移動に伴う空白マスの移動方向を番号の増減を1番目の要素, 列番号の増減を2番目の要素として保持する */
>  5:   Queue: explore_queue  /* 探索対象の盤面を保持するQueueのインスタンスへの参照 */
>  6:   List: check_list  /* 探索済み盤面を保持するListのインスタンスへの参照 */
>  7:   BoardState: state  /* 駒の移動前の盤面を保持するBoardStateのインスタンスへの参照 */
>  8:   BoardState: new_state  /* 駒が移動した後の盤面を保持するBoardStateのインスタンスへの参照 */
>  9:   整数型: change_num  /* 移動する駒の数字 */
> 10:   整数型: i  /* forループ内で使用するカウンタ変数 */
> 11:   start_state ← BoardState()
> 12:   goal_board ← createGoal(board_size)
> 13:   start_state.board ← createStart(board_size)
> 14:   direction ← ((1, 0), (0, -1), (-1, 0), (0, 1))
> 15:   start_state.space ← getSpace(start_state.board)
> 16:   explore_queue ← Queue()
> 17:   explore_queue.add(start_state)
> 18:   check_list ← List()
> 19:   check_list.add(start_state.board)
> 20:   while ( [ア] がfalseと等しい)
> 21:     state ← explore_queue. [イ]
> 22:     for (iを1からdirection要素数まで1ずつ増やす)
> 23:       if (state.space[1] + [ウ] が 1以上 かつ
> 24:           (state.space[2] + [エ] が board_size以下 かつ
> 25:            state.space[2] + [オ] が 1以上))
>              ← 境界チェック（移動先が盤面の範囲内か確認）
> 26:         new_state ← BoardState(state)
> 27:         change_num ← state.board[state.space[1] + [ウ]][state.space[2] + [オ]]
> 28:         new_state.board[state.space[1]][state.space[2]] ← change_num
> 29:         new_state.board[state.space[1] + [ウ]][state.space[2] + [オ]] ← board_size × board_size + 1
> 30:         new_state.space[1] ← state.space[1] + [ウ]
> 31:         new_state.space[2] ← state.space[2] + [オ]
> 32:         new_state.prev ← state
> 33:         if ( [カ] がtrueと等しい)
> 34:           printResult(new_state)
> 35:           return
> 36:         endif
> 37:         if (checkSameBoard(new_state.board, check_list)がfalseと等しい)
> 38:           explore_queue. [キ]
> 39:           check_list. [ク]
> 40:         endif
> 41:       endif
> 42:     endfor
> 43:   endwhile
> 44:   ゴールの盤面に至る駒の移動方法がない旨のメッセージを出力
> 45:   return
> ```

---

## 設問

### 設問1

図2中の②の盤面を図3に倣って、配列で答えよ。

### 設問2

図7中の `[ア]` 〜 `[ク]` に入れる適切な字句を答えよ。

---

## 解答と解説

### 設問1

**正解：**
```
{8, 6, 7}
{2, 9, 4}
{2, 3, 5, 1}  ※ 下記参照
```

**解説：** 図2の1回目のイテレーションでは、ルートノード（開始盤面）から3つの子ノード①②③が生成される。初期盤面の空白マス（board_size²=9 で表現）の位置から方向配列に従い駒を移動した際の②番目の盤面状態を、図3の形式（行ごとの整数型配列の配列）で表現する。

---

### 設問2

**正解：**

| 空欄 | 正解 | 説明 |
|---|---|---|
| **ア** | `explore_queue.isEmpty()` | whileループの継続条件。キューが空でない間（isEmpty()がfalseの間）探索を続ける |
| **イ** | `poll()` | キューの先頭要素を取り出す（FIFO：幅優先探索の核心） |
| **ウ** | `direction[i][1]` | 空白マスの移動方向の行成分（direction配列の1番目の要素） |
| **エ** | `board_size` | 空白移動先の列が盤面内かチェック（board_size以下であることを確認） |
| **オ** | `direction[i][2]` | 空白マスの移動方向の列成分（direction配列の2番目の要素） |
| **カ** | `checkGoal(new_state.board, goal_board)` | 新盤面がゴールに等しいかチェック |
| **キ** | `add(new_state)` | 探索キューに新状態を追加（次のイテレーションで探索対象にする） |
| **ク** | `add(new_state.board)` | 探索済みリストに新盤面を追加（再探索防止） |

**幅優先探索のアルゴリズム解説：**

```
BFS（幅優先探索）の概念:
  探索キュー(FIFO)を使い、ルートから近い順に全ノードを探索する。
  → 最初にゴールに到達した経路 = 最小手数（最小解）
  
  while（キューが空でない）:
    キューから先頭の状態を取り出す → poll()  [イ]
    for（各移動方向）:
      新状態を生成
      if（ゴール？）→ 終了
      if（未探索？）→ キューに追加 [キ]、探索済みリストに追加 [ク]
```

**direction配列の意味：**
```
direction ← ((1, 0), (0, -1), (-1, 0), (0, 1))
           = {下移動, 左移動, 上移動, 右移動}
         ※ direction[i][1] = 行方向の増減 [ウ]
            direction[i][2] = 列方向の増減 [オ]
```

**境界チェック（line 23-25）：**
```
空白マスの移動先が盤面内かを確認:
  行: space[1] + direction[i][1] ≥ 1 (上端チェック)
      space[1] + direction[i][1] ≤ board_size (下端チェック)
  列: space[2] + direction[i][2] ≥ 1 (左端チェック)
      space[2] + direction[i][2] ≤ board_size [エ] (右端チェック)
```

---

## 参考：主要キーワード

| 用語 | 説明 |
|------|------|
| スライドパズル（15パズル等） | N×N-1枚の駒と1つの空白マスを持つパズル。最小手数でゴール配置にする |
| 幅優先探索（BFS: Breadth-First Search） | グラフをルートに近い順に探索する手法。最短経路（最小手数）を保証する |
| キュー（Queue） | FIFO（先入れ先出し）データ構造。BFSの実装に不可欠 |
| ノード | グラフの頂点。スライドパズルでは各盤面状態に対応 |
| エッジ | ノード間の繋がり。スライドパズルでは1手の移動に対応 |
| ゴールノード | ゴール盤面に対応するノード。BFSがここに到達したときが最小解 |
| 終端ノード | 探索済みノードと同じ盤面、またはゴールノードとして探索を打ち切るノード |
| direction配列 | 移動方向（行/列の増減）を定義した配列。(1,0)=下, (0,-1)=左, (-1,0)=上, (0,1)=右 |
| explore_queue | 次に探索すべき状態を管理するFIFOキュー |
| check_list | 探索済み盤面のリスト。同じ盤面の再探索を防ぐ |
| poll() | キューから先頭要素を取り出すメソッド |
| isEmpty() | キューが空かどうかを返すメソッド。falseの間BFSを継続 |
