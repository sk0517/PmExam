# 修正メモ：2021春_問08_情報システム開発.md

照合元：`pdfs/2021r03h_ap_pm_qs.pdf` p.40〜44（問8本体）、公式解答 `pdfs/2021r03h_ap_pm_ans.pdf`
※**解答誤り3件**：設問1(1)＝99.77（MDは「96.20」）、設問3(1)（MDは c=3/d=4/e=5/f=6 で誤り。正 c=2・d/e=3と4,5・f=6）、設問3(2)＝4,6,7,5（MDは「4,6,5,7」）。**原因は表1の処理時間の誤変換**（処理4/6/7）。

## A. 重大（設問・解答に影響）

1. **表1の処理時間の誤変換（行37、39、40）— 設問1(1)・設問3(2)の根拠**
   - PDF表1の正しい値：
     - 処理4「QRコードの画像データを画像ファイルに**書き出し**」処理3完了、**3ミリ秒**、I/O（MDは「10」で誤り）。
     - 処理5「クーポン券番号を発行履歴としてデータベースに書き込み」処理2完了、15、I/O（MD正）。
     - 処理6「スマホアプリにQRコードの画像ファイルを**返信**」処理4完了、**5ミリ秒**、I/O（MDは「2」で誤り）。
     - 処理7「QRコードの画像ファイルを削除」処理6完了、**2ミリ秒**、I/O（MDは「10」で誤り）。
   - MDの処理内容の字句（処理4「書き込む」、処理6「送信」）も公式（書き出し／返信）に合わせる。

2. **設問1(1)の解答誤り（行111〜123）**
   - 公式解答：**99.77（％）**。MDの「96.20」は誤り（かつ解説は99.83/99.75と混乱）。
   - ブロッキングI/Oは全処理を順次実行。総処理時間＝0.02＋10＋0.06＋3＋15＋5＋2＝**35.08ミリ秒**。I/O処理の完了待ち時間合計＝10＋3＋15＋5＋2＝**35.00ミリ秒**。割合＝35.00÷35.08×100≒**99.77％**。解説を全面修正。

3. **設問3(1)の解答誤り（行149〜159）**
   - 公式解答：**c＝2、f＝6**、d・eは2通りの群（同じ群で揃える）：
     - a群：**d＝3、e＝4,5**
     - b群：**d＝3,5、e＝4**
   - MDの「c=3、d=4、e=5、f=6」は図2の空欄位置を誤読。正しい図2構造：
     - メイン処理：処理1 → 処理 **c（=2）**（クーポン券番号取得のI/O開始）
     - ハンドラ1（処理2完了時）：処理 **d** と 処理 **e**（処理3のCPU処理と、処理4・処理5のI/O開始。処理5は処理2依存なのでハンドラ1で処理3と同群にもできる＝a群/b群の2通り）
     - ハンドラ2（処理4完了時）：処理 **f（=6）**（返信のI/O開始）
     - ハンドラ3（処理6完了時）：処理7
   - 解説を公式の群分けに書き直し。

4. **設問3(2)の解答誤り（行161〜173）**
   - 公式解答：**4, 6, 7, 5**。MDの「4, 6, 5, 7」は誤り（表1の誤った処理時間に基づく）。
   - 処理3完了時点をt=0とすると、処理4（3ms）と処理5（15ms）が並行開始：
     - 処理4：t=3で完了
     - 処理6：処理4完了後に開始（5ms）→ t=3+5=8で完了
     - 処理7：処理6完了後に開始（2ms）→ t=8+2=10で完了
     - 処理5：t=15で完了
   - 完了順＝**処理4(3)→処理6(8)→処理7(10)→処理5(15)＝4, 6, 7, 5**。解説を全面修正。

## B. 中程度（本文・字句）

1. **設問2(2)の公式字句（行141〜143）**：公式解答＝「**可読性が下がるから**」。MDの「処理の流れが把握しにくいから」は主旨は合うが、公式字句（可読性が下がる）へ寄せる。

2. **図2の転記（行70〜73）**：MDの箇条書きは概ね合うが、空欄c/d/e/fの位置（メイン処理の処理c=2、ハンドラ1のd/e、ハンドラ2のf=6）を上記A-3に合わせて明確化。

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1) a | 99.77 | 96.20 | **×要修正（A-2）** |
| 設問1(2) b | コンテキスト | コンテキスト | ○ |
| 設問2(1) | ア（callback関数） | ア | ○ |
| 設問2(2) | 可読性が下がるから | 「処理の流れが把握しにくいから」 | ○（公式字句へ、B-1） |
| 設問3(1) c | 2 | 3 | **×要修正（A-3）** |
| 設問3(1) d | 3（a群）／3,5（b群） | 4 | **×要修正（A-3）** |
| 設問3(1) e | 4,5（a群）／4（b群） | 5 | **×要修正（A-3）** |
| 設問3(1) f | 6 | 6 | ○ |
| 設問3(2) | 4, 6, 7, 5 | 4, 6, 5, 7 | **×要修正（A-4）** |

## D. その他・画像確認

- 画像参照 `../images/2021春_問08_表01・図01・図02` の実在と内容を修正時に確認（**表1の処理時間＝処理4:3・処理6:5・処理7:2**、図2の処理c/d/e/fの配置）。
- キーワード表（行179〜188）は妥当。
