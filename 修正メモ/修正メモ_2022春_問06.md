# 修正メモ：2022春_問06_データベース.md

照合元：`pdfs/2022r04h_ap_pm_qs.pdf` p.30〜34（問6本体）、公式解答 `pdfs/2022r04h_ap_pm_ans.pdf`
※**設問3のf・gが逆転（正: f＝SET 発行済枚数 = 発行済枚数 + 1、g＝発行上限枚数）**。図5のSQLはSET/WHEREの順序が入れ替わりINSERT行も欠落。図2は空欄dの位置が崩壊（dは文頭のALTER TABLE）。「1枚限り」が全体で「1枚取り」に誤変換。表2は「キャンセルすると別の予約に利用できる」が真逆＋設問1(3)の根拠文「1枚のクーポンは一つの予約だけに利用できる」が崩壊。本文後半の段落が冒頭に混入。

## A. 重大（設問・解答に影響）

1. **設問3 f・gの逆転（行108〜114、147、186〜196）**
   - 正しい図5：
     ```
     UPDATE クーポン管理 [ f ]
       WHERE クーポンコード = :クーポンコード AND 発行済枚数 < [ g ] ;
     INSERT INTO クーポン明細 (クーポンコード, クーポン発行連番, 獲得会員コード, 獲得制限_1枚限り)
     SELECT :クーポンコード, 発行済枚数, :会員コード, 獲得制限_1枚限り
       FROM クーポン管理 WHERE クーポンコード = :クーポンコード;
     ```
   - 公式解答：**f＝SET 発行済枚数 = 発行済枚数 + 1、g＝発行上限枚数**。MDは図5のSET/WHEREが逆順でfがWHERE側・gがSET側になり、解答も「f=発行上限枚数、g=SET…」と逆。**INSERT INTO クーポン明細 の行も欠落**（SELECTだけが浮いている）。
   - 解説書き直し：UPDATEのSET句で発行済枚数をカウントアップ（f）、WHERE条件の「発行済枚数 < 発行上限枚数」（g）で上限到達後は更新させない。更新後の発行済枚数がそのままクーポン発行連番になる。

2. **図2の空欄d位置の崩壊（行55、61〜64）**
   - 正しい図2：
     ```
     [ d ] クーポン明細 ADD CONSTRAINT クーポン明細_IX1
     UNIQUE(クーポンコード , 獲得会員コード , 獲得制限_1枚限り)
     ```
   - MDは「ALTER TABLE」を平文で埋め込み、空欄dをUNIQUE列リスト内に移動し、列「**獲得会員コード**」を欠落させている。制約名も IK1→**IX1**。
   - 公式解答 d＝ALTER TABLE（MDの解答自体は一致）。
   - 導入文 正（行55）：「なお、予約サイトデータベースにおいては、**UNIQUE制約を構成する複数の列で一つの列でもNULLの場合は、UNIQUE制約違反とならない**。」（MD「複数の列で一つのUNIQUE制約となり」は崩壊。この仕様は獲得制限_1枚限りがNULLのとき重複獲得可＝制約の核心）

3. **表2（クーポン発行サービスの概要）の崩壊・真逆（行47〜51）— 設問1(3)の根拠**
   - 正：
     | 利用局面 | 概要 |
     |---|---|
     | クーポンの照会 | ・発行予定及び発行中クーポンの情報は、会員向けのメール配信によって会員に周知され、施設予約サービスにおいて検索、照会ができる。 |
     | クーポンの獲得 | ・発行中のクーポンを利用するためには、会員がクーポン獲得を行う必要がある。<br>・クーポン獲得を行える期間は定められている。<br>・クーポンの発行枚数が上限に達すると、以降の獲得はできない。 |
     | クーポンの利用 | ・獲得したクーポンは、施設予約サービスにおいて料金の割引に利用できる。<br>・**1枚のクーポンは一つの予約だけに利用できる。**<br>・**クーポンを利用した予約をキャンセルすると、そのクーポンを別の予約に利用できる。**<br>・クーポンの利用期間は定められていて、期限を過ぎたクーポンは無効となる。 |
   - MDの「クーポンの概要」行は捏造、「キャンセルされた場合、そのクーポンは再利用できない」は**真逆**、設問1(3)の解答字句「1枚のクーポンは一つの予約だけに利用できる。」が崩壊している。

4. **表1（チェック仕様）の崩壊（行36〜39）**
   - 正：
     | 提携スポンサ | クーポンの獲得制限 | クーポンを利用した予約制限 |
     |---|---|---|
     | 施設 | ・同一会員による同一クーポンの獲得可能枚数を、1枚に制限する（以下、"**同一会員1枚限りの獲得制限**"という）。 | ・設定した施設だけを予約可能にする。<br>・利用金額が設定金額以上の予約だけを可能にする。 |
     | 都道府県、市区町村 | ・設定地区に居住する会員だけが獲得可能にする。 | ・**設定地区にある施設だけを予約可能にする。** |
   - MDはスポンサ列が「施設、及び都道府県、市区町村」と混線し、都道府県行の予約制限が「—」で欠落。

5. **冒頭への後半段落の混入＋冒頭段落の欠落（行10〜14）**
   - MD行12〜14（トラフィック集中・ロックなし方式・COALESCE説明）は本文後半（p.33、〔クーポン獲得処理の連番管理方式〕内）の文章であり、冒頭から削除して正位置（行82の後）へ移す。
   - 正しい冒頭（行10の後に復元）：「発行するクーポンには割引金額が設定されていて、施設予約の際に料金の割引に利用することができる。K社は、施設、又は都道府県、若しくは市区町村を提携スポンサとして、提携スポンサと合意した割引金額、枚数のクーポンを発行する。」「クーポン発行に関しては、提携スポンサによって各種制限が設けられているので、クーポンの獲得、及びクーポンを利用した予約の際に、制限が満たされていることをチェックする仕組みを用意する。」「提携スポンサによって任意に設定可能なチェック仕様の一部を表1に、クーポン発行サービスの概要を表2に示す。」
   - 正位置の文 正：「…特に、提携スポンサが都道府県、市区町村であるクーポンは割引金額が大きく、クーポンの発行直後にトラフィックが集中することが**予想**される。**発行上限枚数到達後に**クーポン獲得処理が動作する場合の考慮も必要である。**L主任**は、トラフィック集中時のリソース競合による**レスポンス悪化**を懸念して、ロック解放待ちを発生させない連番管理方式（以下、ロックなし方式という）のSQL文（図4）を**考案**した。」（MD「K社は」「選定した」「発行に限数」は誤り）

6. **図4（ロックなし方式SQL）の全面崩壊（行90〜94）— 設問2の文脈**
   - 正：
     ```
     INSERT INTO クーポン明細 (クーポンコード, クーポン発行連番, 獲得会員コード, 獲得制限_1枚限り)
     WITH 発行済枚数取得 AS (SELECT COALESCE(MAX( [ e ] ), 0) AS 発行済枚数
       FROM クーポン明細 WHERE クーポンコード = :クーポンコード)
     SELECT :クーポンコード,
           (SELECT 発行済枚数 + 1 FROM 発行済枚数取得 WHERE
             (SELECT 発行済枚数 FROM 発行済枚数取得) < 発行上限枚数),
           :会員コード, 獲得制限_1枚限り
       FROM クーポン管理 WHERE クーポンコード = :クーポンコード
     ```
   - MDのSQLは構文が崩壊（WITH句消失、列名「獲得制限_会員コード」捏造）。公式解答 e＝クーポン発行連番（MD一致）。

7. **表3（方式比較）の崩壊（行126〜129）**
   - 正（列＝ロック解放待ち／主キー制約違反による再オペレーション／発行上限枚数に到達後の動作）：
     | 方式 | ロック解放待ち | 主キー制約違反による再オペレーション | 発行上限枚数に到達後の動作 |
     |---|---|---|---|
     | ロックなし | 発生しない | 発生する | 副問合せで取得する発行済枚数＋1の値がNULLになり、クーポン明細テーブルのクーポン発行連番がNULLのレコードを追加しようとして、主キー制約違反となる。 |
     | ロックあり | 発生する | 発生しない | 更新が行われず、クーポン明細テーブルのクーポン発行連番が `[g]` のレコードを追加しようとして、主キー制約違反となる。 |
   - 注記：「表3中の `[g]` には、図5中の `[g]` と同じ字句が入る。」（MDでは注記ごと欠落）
   - 末尾 正（行131）：「…そこで、**会員による再オペレーションの発生しない**ロックあり方式を採用することにした。」

8. **下線番号の誤り（行66、118、149）**
   - 行66 正：「L主任は、**①予約テーブルの"クーポンコード"、"クーポン発行連番"に対しても、UNIQUE制約を付けた。**」（下線①はこの文。「も」欠落）
   - 行118 正：「**④ロックあり方式では、図3のCRUD図の一部に変更が発生する。**」（MDは「③」）
   - 設問4 正：「本文中の**下線④**について、図3中の下線②、下線③の変更後のレコード操作内容を、注記に従いそれぞれ答えよ。」（図3のクーポン獲得行に下線②R〔クーポン管理〕・下線③CR〔クーポン明細〕がある）

## B. 中程度（本文の誤変換）

1. **冒頭（行10）**：正「K社は、インターネットでホテル…各施設は**幾つかの利用プラン**を提供していて」
2. **獲得制限列の説明（行22）**：正「クーポン管理テーブルの列名の先頭に**"獲得制限"又は"予約制限"**が付く列は、クーポンの獲得制限、又はクーポンを利用した予約制限のチェック処理で使用し、**チェックが必要ない場合にはNULLを設定する**。」（MD「チェックが必要な場合はNULL」は真逆）。「獲得制限_1枚**限り**」「同一会員1枚**限り**の獲得制限」（「取り」誤変換の全置換。行22・38・55・57・63・91・112ほか全箇所）
3. **ロックの説明（行68）**：正「…レコード単位にロックを掛け、**多重処理を行う設定**としている。ロックが掛かると**トランザクションが終了するまでの間、他のトランザクションによる同一レコードに対する処理はロック解放待ちとなる**。」（MD「多重処理を方言設定」は崩壊）
4. **クーポン獲得チェック（行74）**：正「クーポン発行サービスと施設予約サービスのCRUD図（抜粋）を図3に示す。」（欠落）＋「クーポン新規登録処理では、1種類のクーポンにつき1レコードを…。クーポン獲得チェック処理では、獲得可能期間、**会員住所による獲得制限、発行上限枚数**に関するチェックを行う。**チェックの結果、エラーがない場合に表示される同意ボタン**を押すことによって、クーポン獲得処理を行う。」（MD「エラーが発生した場合に表示される」は真逆）
5. **M課長の指摘（行98）**：正「…ロック解放待ちを発生させることによって更新が**順次**行われる連番管理方式（以下、ロックあり方式という）の**検討と方式の比較、高負荷試験の実施を指示した**。」
6. **設問1(1)の解説（行157〜163）**：a・bは**予約エンティティ内の空欄属性**（a＝施設コード、b＝プランコード、順不同。予約→利用プランを参照する外部キーで破線下線付き）、c＝**利用プランと施設の間の関連**（施設→利用プランの1対多、図では↑）。MDの「クーポン管理→施設への参照キー」という説明は誤りなので書き直し。

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1) a | 施設コード（bと順不同） | 同一 | ○（解説をB-6で修正） |
| 設問1(1) b | プランコード | 同一 | ○ |
| 設問1(1) c | ↑（施設と利用プランの1対多） | 同一 | ○ |
| 設問1(2) d | ALTER TABLE | 同一 | ○（**図2の空欄位置をA-2で修正**） |
| 設問1(3) | 1枚のクーポンは一つの予約だけに利用できる。 | 同一 | ○（根拠の表2をA-3で復元） |
| 設問2 e | クーポン発行連番 | 同一 | ○（図4をA-6で復元） |
| 設問3 f | SET 発行済枚数 = 発行済枚数 + 1 | 「発行上限枚数」 | **×要修正（A-1）f/g逆転** |
| 設問3 g | 発行上限枚数 | 「SET 発行済枚数 = 発行済枚数 + 1」 | **×要修正（A-1）f/g逆転** |
| 設問4 下線② | RU | 同一 | ○（設問文の下線④をA-8で修正） |
| 設問4 下線③ | C | 「C」だが解説の「CR→C」文脈が不明瞭 | ○（図3のクーポン獲得行＝②R・③CRであることを明記） |

## D. その他・画像確認

- 画像参照 `../images/2022春_問06_図01〜図05・表01〜表03` の実在と内容を修正時に確認（図5はUPDATE文のSET位置にf・WHERE条件にg、図3クーポン獲得行に下線②R／③CRがあること）。
- キーワード表の「ロックあり方式＝SELECT FOR UPDATE」は本問の方式（UPDATEによる行ロック）と異なるので説明を修正。
