# 修正メモ：2023秋_問08_情報システム開発.md

照合元：`pdfs/2023r05a_ap_pm_qs.pdf` p.42〜45（問8本体）、公式解答 `pdfs/2023r05a_ap_pm_ans.pdf`
※解答の記号・値は公式と一致。ただし**空欄c・dを定義する段落（プロセス／スレッドの説明）が丸ごと欠落**し、下線①②の位置が捏造（下線②は「メインスレッド」、MDは「禁止」に②を振る）。**設問2(2)の解答群が4択→3択に欠落（イ「レスポンスを待つ処理」消失）**、表1の処理内容も誤変換が多い。

## A. 重大（設問・解答の根拠に影響）

1. **空欄c・dの本文段落の丸ごと欠落＋下線①②の捏造（行43〜45）**
   - 正しい構成（3段落に差し替え）：
     - 「Cさんは、Java言語を用いてスマホアプリのトップ画面の開発に着手した。**トップ画面を実装し、画面の描画処理の中で、顧客番号に関連付けられた顧客氏名、来店日付、担当美容師氏名の情報を**サーバから取得して画面に表示する処理を行うようにした。しかし、このスマホアプリを実行したところ**並行処理に関するエラー（例外）**が発生し、スマホアプリの実行が中断された。」
     - 「このエラーの原因を究明するために、スマートフォン上で動作するGUIアプリケーションにおける並行処理を行う仕組みに関して調査を行った。**スマートフォンのOS上で処理を実行するための仕組みとして `[c]` と `[d]` とがある。`[c]` は、独立したメモリ空間を割り当てて実行されるものであり、多くの場合アプリケーションの実行単位ごとに一つの `[c]` で実行される。一方 `[d]` は、一つのメモリ空間を共有しながら実行されるもので、一つの `[c]` の中で、複数の `[d]` を実行することができる。**」（設問2(1)の直接根拠。MDでは丸ごと欠落）
     - 「GUIアプリケーションの開発では、画面描画、画面操作などの画面ユーザーインタフェースに関する処理を行うメインスレッドと、メインスレッドと並行して比較的処理時間が長い処理を行う<u>①バックグラウンドスレッド（以下、ワーカースレッドという）</u>とを分けて実装する必要がある。また、**ワーカースレッドによる画面ユーザーインタフェースに関する処理は禁止されている**ことが分かった。」（下線は①のみ。MDの「②禁止」は捏造）

2. **設問2(2)の解答群の欠落＋設問文の下線番号誤り（行93〜98）**
   - 設問文 正：「本文中の**下線①**について、ワーカースレッドで実行すべき**ではない**処理を解答群の中から選び、記号で答えよ。」（MD「下線②」）
   - 正しい4択：
     - ア サーバから取得した情報を画面に表示する処理（＝公式解答）
     - **イ サーバからのレスポンスを待つ処理**（MDで欠落）
     - ウ サーバへリクエストを送信する処理
     - エ ホスト名からIPアドレスを取得しTCPコネクションを確立する処理
   - MDは3択で記号が1つずつずれている。

3. **下線②の位置と待ち合わせ段落の崩壊（行69）— 設問3(1)(2)の根拠**
   - 正：「表1の七つの処理を行うために、<u>②**メインスレッド**</u>と二つのワーカースレッドを作成して処理を行うプログラムを実装した。処理4と処理5は並行に実行できるので、別々のワーカースレッドで処理することにした。このとき、**処理6の実行の開始条件は処理4と処理5が共に完了していることなので**、二つのスレッドの完了を待ち合わせる `[e]` 操作を**処理6のプログラムに記載した**。」
   - 設問3(1)の設問文 正：「本文中の**下線②**について、表1中の処理2〜処理7のうち**メインスレッドで実行すべき処理だけを、表1中の処理名で**全て答えよ。」（MD「下線①」）

4. **表1（処理一覧）の誤変換（行61〜67）**
   - 正：
     | 処理名 | 処理内容 | 開始条件 | 処理時間(ミリ秒) |
     |---|---|---|---|
     | 処理1 | スマートフォンのカメラデバイスから取得したカメラ映像を画面に表示して、顧客が撮影ボタンを押した時点の画像を**顔写真**として保存する。 | なし | 100 |
     | 処理2 | 画面に"処理中"のメッセージを表示する。 | 処理1の完了 | 10 |
     | 処理3 | **処理1で保存した顔写真から顔の特徴点を抽出する。** | 処理1の完了 | 100 |
     | 処理4 | **処理1で保存した顔写真から毛髪部分を削除する画像処理を行う。** | 処理3の完了 | 150 |
     | 処理5 | 処理3で抽出した特徴点をサーバに送信し、**おすすめの髪型の画像を取得する**。 | 処理3の完了 | 200 |
     | 処理6 | **処理4の結果画像と処理5の画像を合成する。** | 処理4、処理5の完了 | 50 |
     | 処理7 | 処理6で合成した**写真**を画面に表示する。 | 処理6の完了 | 10 |
   - MDの「静止画」「顔部分の情報」「髪型の候補」「処理4の合成画像と処理5の情報」等は誤り。

## B. 中程度（本文の誤変換）

1. **冒頭（行10）**：正「B社の**全店舗**で顧客カードを持参した**顧客に**割引価格でサービスを提供している。…売上げが減少**傾向**にある。」
2. **図1（行26〜29）**：来店予約 正「店名、日付、**希望美容師**、希望コース（カット、パーマなど）を指定して予約可能時間を検索し、来店予約を行う機能と予約情報を確認、変更する機能。」（MD「希望施術内容」は捏造）。お知らせ 正「キャンペーン、**新商品の入荷**などの情報を知らせる機能。」おすすめの髪型 正「スマートフォンのカメラで**撮影した**顧客の顔の特徴情報を基に…」。トップ画面の内容（顧客番号：99999999、○○様、ご来店：20XX年XX月XX日、担当美容師、来店予約／お知らせ／クーポン／おすすめの髪型の4ボタン）も注記に追加。
3. **アイディア（行18）**：原文表記は「アイディア」。
4. **おすすめの髪型機能（行51・53）**：見出し・本文とも原文は「**おすすめの髪型機能**」（MD「おすすめ髪型機能」）。「なお、表1中の開始条件とは当該処理の実行を開始する**ために必要な条件**であり…」
5. **通信環境の段落（行71）**：正「Cさんは、処理1〜処理7で構成されるおすすめの髪型機能を実装して**テスト用に準備した**スマートフォンで実行したところ…」
6. **設問3(4)（行110）**：正「…ここで、通信は標準的な時間で実行でき、表1に記載の処理時間以外については無視できるものとする。」
7. **設問2(1)の解説（行131〜132）**：cの解説を「独立したメモリ空間を割り当てて実行される実行単位＝プロセス」、dを「一つのメモリ空間を共有し、1プロセス内で複数実行できる＝スレッド」と、復元した本文（A-1）に基づく説明に書き直し。

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 a | イ（Webアプリケーション） | イ | ○ |
| 設問1 b | エ（ネイティブアプリケーション） | エ | ○ |
| 設問2(1) c | エ（プロセス） | エ | ○（**根拠の本文段落をA-1で復元**） |
| 設問2(1) d | ウ（スレッド） | ウ | ○ |
| 設問2(2) | ア | 「ア」だが解答群3択に欠落・設問文の下線番号誤り | **×要修正（A-2）** |
| 設問3(1) | 処理2、処理7 | 同一 | ○（設問文の下線番号をA-3で修正） |
| 設問3(2) e | ウ（join） | ウ | ○ |
| 設問3(3) | 一定時間でタイムアウトする処理 | 同一 | ○ |
| 設問3(4) | 460（ミリ秒） | 同一 | ○ |

## D. その他・画像確認

- 画像参照 `../images/2023秋_問08_図01・表01` の実在と内容を修正時に確認。
- キーワード表の「PWM | 関係なし（問7参照）」の行は削除（この問題に無関係な混入）。
