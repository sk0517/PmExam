# 修正メモ：2025春_問03_プログラミング.md

対象ファイル: `2025春/2025春_問03_プログラミング.md`
照合元: `pdfs/2025r07h_ap_pm_qs.pdf`（問題, p.18〜23）、`pdfs/2025r07h_ap_pm_ans.pdf`（IPA公式解答例, p.2）

設問2の解答（ア〜ク）は公式と一致しているが、**設問1の解答が誤り**、**図1・図2・図3・図7（擬似コード）の内容が原本と大きく食い違っている**。プログラミング問題は本文＝コードそのものなので、図7の誤りは致命的。行番号は現行MDのもの。

---

## A. 重大（解答・コードの誤り）

### A-1. 設問1の解答が誤り（192〜197行目）
- 現状: `{8, 6, 7}` `{2, 9, 4}` `{2, 3, 5, 1}`（3行目が4要素になっており明らかに壊れている）
- **IPA公式解答: `{{8, 6, 7}, {2, 9, 4}, {3, 5, 1}}`**
- 解説も書き直し: ルートノード（8 6 7 / 2 5 4 / 3 空白 1、空白は(3,2)）から②は空白マスが上（行-1）へ移動したケース。駒5が下りて空白（値9）が(2,2)に来るので {{8,6,7},{2,9,4},{3,5,1}}。

### A-2. 図1の開始盤面が全く別物（29〜40行目）
- 現状: 開始盤面「8 1 3 / 4 _ 2 / 7 6 5」
- 正（PDF）: 開始時点の盤面「**8 6 7 / 2 5 4 / 3 空白 1**」（1行目=8,6,7 2行目=2,5,4 3行目=3,空白,1。空白は3行目2列目）
- ゴールの盤面「1 2 3 / 4 5 6 / 7 8 空白」は現状OK。
- ※この開始盤面は図2・図3・設問1すべての前提。必ず修正。

### A-3. 図7擬似コードがPDFと構造ごと異なる（125〜172行目）
PDF原本（p.22〜23）の正しいコード:

```
 1: ○solveNPuzzle(整数型: board_size)
 2:   BoardState: start_state
 3:   整数型配列の配列: goal_board
 4:   整数型配列の配列: direction
 5:   Queue: explore_queue
 6:   List: check_list
 7:   BoardState: state
 8:   BoardState: new_state
 9:   整数型: change_num
10:   整数型: i
11:   start_state ← BoardState()
12:   goal_board ← createGoal(board_size)
13:   start_state.board ← createStart(board_size)
14:   start_state.space ← getSpace(start_state.board)
15:   direction ← {{1, 0}, {0, -1}, {-1, 0}, {0, 1}}
16:   explore_queue ← Queue()
17:   explore_queue.add(start_state)
18:   check_list ← List()
19:   check_list.add(start_state.board)
20:   while ( [ア] がfalseと等しい)
21:     state ← explore_queue.[イ]
22:     for (iを1からdirectionの要素数まで1ずつ増やす)
23:       if ((state.space[1] + [ウ] が1以上) かつ
24:           (state.space[1] + [ウ] が [エ] 以下) かつ
25:           (state.space[2] + [オ] が1以上) かつ
26:           (state.space[2] + [オ] が [エ] 以下))
27:         new_state ← BoardState(state)
28:         new_state.space[1] ← state.space[1] + [ウ]
29:         new_state.space[2] ← state.space[2] + [オ]
30:         change_num ← new_state.board[new_state.space[1]][new_state.space[2]]
31:         new_state.board[new_state.space[1]][new_state.space[2]] ← board_size × board_size
32:         new_state.board[state.space[1]][state.space[2]] ← change_num
33:         if ( [カ] がtrueと等しい)
34:           printResult(new_state)
35:           return
36:         endif
37:         if (checkSameBoard(new_state.board, check_list)がfalseと等しい)
38:           explore_queue.[キ]
39:           check_list.[ク]
40:         endif
41:       endif
42:     endfor
43:   endwhile
44:   ゴールの盤面に至る駒の移動方法がない旨のメッセージを出力
45:   return
```

現行MDとの主な相違:
1. **関数名**: 正は `solveNPuzzle`（MDは `solvePuzzle`）
2. **13〜15行の順序**: 正は board→space→direction の順（MDは direction が先）
3. **境界チェック（23〜26行）が4条件**: 正は「行が1以上」「行がエ以下」「列が1以上」「列がエ以下」の4つ。MDは3条件に潰れ、空欄エの位置も誤り（エは2箇所に登場する。空欄ウも2箇所）
4. **駒移動の処理順序（27〜32行）**: 正は「space更新→change_num取得→空白値代入→駒書き戻し」の順で、change_numは `new_state.board[new_state.space[1]][new_state.space[2]]` から取得。MDは別ロジックに書き換わっている
5. **空白マスの値**: 正は `board_size × board_size`（MDは `board_size × board_size + 1` と誤り。図3でも空白=9=3×3で整合）
6. **`new_state.prev ← state` という行は存在しない**（MDが創作。prevはコンストラクタ `BoardState(state)` が設定する——図4の説明(3)のとおり）

### A-4. 図2の説明が誤り（60〜69行目）
- 正（PDF図2）: ルートノードから1回目で子ノード**①②③**を作成（②は網掛けで非表示＝設問1の対象）。2回目は**①〜③から④〜⑪の8個**の子ノードを作成し、盤面が探索済みノードと一致する**⑤、⑥及び⑩が終端ノード**（×印）。**⑫**がn回目でゴールノードに至ったことを示す。
- 現状MDは「①から④〜⑥を作成」「②③は探索済みにより終端」「図2の⑧は」と誤っている（(2)(3)の本文も同様に修正）。
- 凡例: 「×:終端ノード」「注記 網掛けの部分は表示していない。」

### A-5. 図3（配列表現）の内容が誤り（79〜90行目）
- 正（PDF図3）:
  - (1) 盤面の行ごとに並ぶ駒の数字を整数型の配列で表す。1行目 {8, 6, 7}／2行目 {2, 5, 4}／3行目 **{3, 9, 1}**（空白マス=9）
  - (2) (1)で行ごとに定義した配列を整数型配列の配列（以下、盤面配列という）で表す。**{{8, 6, 7}, {2, 5, 4}, {3, 9, 1}}**
  - (3) 1行目の3列目の駒の数字は、盤面配列名をboardとすると**board[1][3]**として参照する。
- 現状MDの「0 6 7」「(0は空白=9ではなく仮の値)」「board[i][j]として参照」は誤り。
- 本文73行目: 「空白マスを表す値は**最も大きい駒の数字である8に1を加えた9**とする」（現状「Nに1を加えた値」は誤り）

---

## B. 中程度（欠落・不正確な記述）

### B-1. クラスQueue・クラスListの説明（図5・図6）が丸ごと欠落
設問2のイ（poll()）・キ（add(new_state)）・ク（add(new_state.board)）の解答根拠。以下を追加:

**図5 クラスQueueの説明**
- コンストラクタ Queue(): 可変長のキューを生成する。
- メソッド:
  | メソッド | 戻り値 | 説明 |
  |---|---|---|
  | add(BoardState: st) | なし | キューにBoardStateのインスタンスへの参照stを追加する。 |
  | isEmpty() | 論理型 | キューが空の場合はtrueを返し、空でない場合はfalseを返す。 |
  | poll() | BoardState | 先頭のBoardStateのインスタンスへの参照を取り出して返す。 |
  | peek() | BoardState | 先頭のBoardStateのインスタンスへの参照を返す。 |

**図6 クラスListの説明**
- コンストラクタ List(): 可変長のリストを生成する。
- メソッド:
  | メソッド | 戻り値 | 説明 |
  |---|---|---|
  | add(整数型配列の配列: b) | なし | リストに盤面配列bを追加する。 |
  | isEmpty() | 論理型 | リストが空の場合はtrueを返し、空でない場合はfalseを返す。 |
  | peek() | 整数型配列の配列 | リストの先頭に存在する盤面配列を返す。 |

### B-2. 図4（クラスBoardState）の説明の不正確（102〜107行目）
- space: 正「空白マスの場所を示す**行番号と列番号の二つの要素から成る配列**」（現状「二つの配列からなる配列」は誤り）
- prev: 正「**駒が1回移動する前の**BoardStateのインスタンスへの参照。**開始時点の盤面からの遷移を出力する際に用いる**。初期状態は未定義。」
- コンストラクタの説明が欠落。追加:
  - BoardState(): インスタンスを初期化する。
  - BoardState(BoardState: st): インスタンスを初期化し、(1) boardに引数stのboardの値を格納、(2) spaceに引数stのspaceの値を格納、(3) **prevに引数stへの参照を格納**する。（図7のコード理解に必須）

### B-3. 表2（主な関数）の不備（113〜119行目）
- **printResult(BoardState: st) が欠落**: 「引数として与えられたBoardStateのインスタンスへの参照stを用いて、開始時点の盤面からの遷移及び駒の移動回数を出力する。戻り値なし。」を追加
- createGoal: 正「引数board_sizeの値を一辺のマス数としたスライドパズルのゴールの盤面を表す配列（以下、ゴールの盤面配列という）を作成して返す」
- createStart: 正「…開始時点の盤面配列をランダムに作成し、**出力して返す。なお、ゴールの盤面配列と同じものが作成されることはない**」
- checkGoal: 正「第一引数の盤面配列bと**第二引数のゴールの盤面配列g**を用いて両者が一致しているかどうかを判定して結果を返す。一致する場合はtrueを、一致しない場合はfalseを返す」
- checkSameBoard: 正「引数で渡される**探索済みの盤面配列のリストへの参照l**を用いて、盤面配列bが探索済みの盤面配列のリストに存在するかどうかを判定して結果を返す。存在する場合はtrue、存在しない場合はfalseを返す」
- 見出しの「表2〜表6」「表5」という表番号の振り方も原本に合わせ「図4」「図5」「図6」「表2」に修正

### B-4. 本文の細かい誤り
- 表1「盤面」: 正「正方形の**面**に、1から順に1ずつ大きくなる数字の書かれた駒が配置されている。**1か所だけ駒の置かれていない**空白のマス…」
- 表1「ゴールの盤面」: 正「盤面左上に数字が1の駒があり、右の駒の数字は1ずつ増えていき、その行の右端の駒に書かれた数字より1大きい数字が、次の行の左端の駒の数字となる。盤面右下のマスが空白マスとなる」
- 表1「駒の移動」: 正「駒の上下左右のいずれかに空白マスがある場合、駒を空白マスに移動することができる。駒が移動した場合、駒が置かれていたマスが空白マスになる。駒を空白マスに移動させて盤面を変化させ、ゴールの盤面と同じにすることを目指す」（現状の「駒の移動を1手とする」等は創作）
- 96行目: 「開始時点の盤面をランダムに作成し、最小解を求め、**開始時点の盤面からの遷移及び駒の移動回数**を出力するプログラム」「クラスBoardStateの説明を**図4**に」（現状「表4」）
- 50行目〔方法〕: 正「探索対象のノードに対して、移動できる駒ごとにその駒を移動した後のノードを作成して、**探索対象のノードの子ノードとし**、探索対象のノードは探索済みとなる。このとき、子ノードが表す盤面が**既に探索したノード**（以下、探索済みノードという）と同じであれば…」「最小解が見つかったと**判断**して探索を終了する」

---

## C. 解説の見直し

IPA公式解答例（問3）:

| 設問 | 公式解答 |
|---|---|
| 設問1 | {{8, 6, 7}, {2, 9, 4}, {3, 5, 1}} |
| 設問2 ア | explore_queue.isEmpty() |
| 設問2 イ | poll() |
| 設問2 ウ | direction[i][1] |
| 設問2 エ | board_size |
| 設問2 オ | direction[i][2] |
| 設問2 カ | checkGoal(new_state.board, goal_board) |
| 設問2 キ | add(new_state) |
| 設問2 ク | add(new_state.board) |

- 設問2の解答はすべて公式と一致。ただし解説内のコード引用（境界チェックの4条件、空白値=board_size×board_size、行27〜32の処理順序）を修正後のコードに合わせて直すこと。
- 設問1の解説は全面書き直し（A-1参照）。ポイント: ルートノードの空白は3行目2列目。direction={{1,0},{0,-1},{-1,0},{0,1}}の順に子ノードを試すと、①=空白が下方向…ではなく盤外、実際に可能な移動は3方向で、②は空白が上（-1,0）へ移動したケース（網掛けで隠されている）。駒5と空白が入れ替わり {{8,6,7},{2,9,4},{3,5,1}}（9=空白）。

---

## D. その他

- 画像の切り出し確認: `2025春_問03_図01_パズルの例.png`、`図02_BFS過程.png`、`図03_配列表現.png`、`表01_スライドパズルのルール.png`、`表02_クラス説明.png`、`表05_主な関数.png`、`図07_プログラム.png`。特に図1・図3・図7は説明文が誤っていたため、画像自体の内容・範囲も要確認。
- 表番号・図番号の対応を原本（表1、図1、図2、図3、図4、図5、図6、表2、図7）に統一。
