# 修正メモ：2023春_問03_プログラミング.md

照合元：`pdfs/2023r05h_ap_pm_qs.pdf` p.16〜20（問3本体）、公式解答 `pdfs/2023r05h_ap_pm_ans.pdf`
※解答の値自体は公式と一致（ア=3×7、イ=4×12、①48②260③48④84、ウ〜ケ、2×N）。ただし**図3・図4の擬似コードが全面崩壊しており、空欄ウ〜ケの出現位置がすべて誤り**。図1・表1〜表3・設問2の式も崩壊。解答は合っていても学習素材として成立していないため全面修正が必要。

## A. 重大（設問・解答の根拠に影響）

1. **図3（ツリー構築プログラム）の全面崩壊（行112〜128）— 空欄ウ・エ・オ・カの位置が誤り**
   - 正しい擬似コード（p.19）：
     ```
     // ツリーの各層の、elements配列上での先頭インデックスを算出する
     layer_top[1] ← 1                          // ルートノードは先頭なので1を入れる
     for (iを1からt_depth − 1まで1ずつ増やす)
       layer_top[i + 1] ← layer_top[i] + ［ウ］
     endfor

     // ツリーを構築する
     elements[1] ← new_elem(val1.N, val1, val2)   // ルートノードを用意。桁数はval1の桁数を使う
     for (dpを1からt_depth − 1まで1ずつ増やす)     // ルートノードの層から、最下層以外の層を順に処理
       for (iを1からpow(3, dp − 1)まで1ずつ増やす) // 親ノードになる層の要素数だけ繰り返す
         pe ← elements[layer_top[dp] + (i − 1)]   // 親ノードの要素を取得
         cn ← pe.N / 2                            // 子ノードの桁数を算出
         tidx ← layer_top[dp + 1] + ［エ］        // 子ノード①へのインデックス
         elements[tidx]     ← new_elem(cn, left(［オ］, cn), left(［カ］, cn))
         elements[tidx + 1] ← new_elem(cn, right(［オ］, cn), right(［カ］, cn))
         elements[tidx + 2] ← new_elem(cn, lradd(［オ］, cn), lradd(［カ］, cn))
       endfor
     endfor
     ```
   - **ウは layer_top の算出ループ内**（各層のノード数 `pow(3, i−1)` を加算）。MDはウを tidx 行に置き、エを③の new_elem 引数に置くなど全て誤り。内側forの範囲も正は `pow(3, dp − 1)`（MD `pow(3, dp)`）。オ=pe.val1、カ=pe.val2 は left/right/lradd の第1引数として3行に共通出現。

2. **図4（演算プログラム）の全面崩壊（行134〜159）— 空欄キ・ク・ケの位置が誤り**
   - 正しい擬似コード（p.20）：
     ```
     // 最下層の計算
     for (iを1からpow(3, t_depth − 1)まで1ずつ増やす)  // 最下層の要素数は3のt_depth−1乗個
       el ← elements[layer_top[t_depth] + (i − 1)]     // 最下層のノード
       mul ← el.val1.values[1] * el.val2.values[1]     // 最下層の乗算
       el.result.N ← 2                                 // 計算結果は2桁の多倍長整数
       el.result.values[1] ← ［キ］                    // 1の位
       el.result.values[2] ← mul / 10                  // 10の位
     endfor

     // 最下層以外の計算
     for (dpをt_depth − 1から1まで1ずつ減らす)          // 最下層より一つ上の層から順に処理
       for (iを1からpow(3, dp − 1)まで1ずつ増やす)      // 各層の要素数だけ繰り返す
         el ← elements[layer_top[dp] + (i − 1)]        // 計算対象のノード
         cidx ← layer_top[dp + 1] + ［エ］             // 子ノード①へのインデックス
         s1 ← sub(［ク］.result, ［ケ］.result)
         s2 ← sub(s1, elements[cidx + 1].result)       // γ−α−βを計算
         p1 ← shift(elements[cidx].result, el.N)       // α×10^Kを計算
         p2 ← shift(s2, el.N / 2)                      // (γ−α−β)×10^(K/2)を計算
         p3 ← elements[cidx + 1].result                // βを計算
         el.result ← add(add(p1, p2), p3)
       endfor
     endfor

     // 繰り上がり処理
     answer ← carry(elements[1].result)                // 計算結果をanswerに格納
     ```
   - 注記も復元：「図4中の［エ］には、図3中の［エ］と同じ字句が入る。」
   - MDの `si.result ← 2`、ループ内 `carry` 呼び出し、`sub(..., result, el.N)`（3引数）等はすべて捏造。**キ=mod(mul, 10)＝1の位**（MDの解説は10の位と逆）。ク=elements[cidx＋2]（γ側）、ケ=elements[cidx]（α側）で s1=γ−α → s2=γ−α−β の流れ。

3. **設問2の式が崩壊（行167〜169）— 空欄は4個で「84」は所与**
   - 正：「次の式の①〜④に入れる適切な整数を答えよ。
     `（①）×100 ＋（（②）−（③）−84）×10 ＋（④）`」
   - MDの「①〜⑥」「`(①)×(②)+((③)−(④)−(⑤))×(⑥)+(⑦)`」は捏造。公式解答 ①=48、②=260、③=48、④=84 はMDと一致。解説を書き直す：46×134のノードでは子ノードが①4×12（α=48）、②6×14（β=84）、③10×26（γ=260）、K=2なので 48×100＋(260−48−84)×10＋84＝7006652の部分計算6164ではなく…（正しくは 48×100+128×10+84=6164）。
   - MD解説（行202〜216）の「A=4, B=6, C=1, D=34」等の試行錯誤文は削除し、上記の正しい導出に差し替え。

4. **図2の説明崩壊（行37・43〜49）— ア・イの位置が消失（設問1の対象）**
   - 正しい図2の構成：
     - 層1：`1234 × 5678` → A:12 B:34 C:56 D:78
     - 層2：①`12×56 (A×C)`、②`34×78 (B×D)`、③`46×134 ((A+B)×(C+D))`
     - 層3：①の子＝`1×5`・`2×6`・`3×11`／②の子＝**［ア］**・`4×8`・`7×15`／③の子＝**［イ］**・`6×14`・`10×26`
     - 注記：この例では層3が最下層となる。
   - MDの図2説明にはア・イが存在せず設問1が解けない。復元必須。
   - 図2直後の文 正：「図2の層2の場合、①は12×56、②は34×78、③は46×134となる。**③の(C+D)は、"桁ごとの加算"だけの処理を行うと、10の位が5+7=12、1の位が6+8=14となるので、12×10+14=134となる。**」（MDの「A+B=12+34=46, C+D=56+78=134」は誤り。C+D=56+78の桁ごと加算）
   - 設問1の解説も書き直し：ア＝②34×78の子①＝A×C＝**3×7**／イ＝③46×134の子①＝A×C＝**4×12**（46→A=4,B=6、134→10の位12・1の位14でC=12,D=14）。MD解説（行185〜194「21ノード」「48」）は意味不明のため全削除。

5. **〔ツリー構造の構築〕節の欠落・崩壊（行35）**
   - 見出し〔ツリー構造の構築〕がMDでは消失（〔多倍長整数の乗算〕に混入）。正：
     - 「カラツバ法を適用した乗算のアルゴリズムは、**計算のためのツリー構造（以下、ツリーという）を作る処理と、ツリーを用いて演算をする処理から成る。ツリーは、多倍長整数の乗算の式を一つのノードとし、一つのノードは3個の子ノードをもつ。**」
     - 「M桁×M桁の乗算の式について、**乗算記号の左右にある値**を、それぞれM/2桁ずつに分けてA、B、C、Dの四つの多倍長整数を作る。これらの整数を使って、①A×C、②B×D、③(A+B)×(C+D)の3個の子ノードを作り、M/2桁×M/2桁の乗算を行う層を作る。**(A+B)、(C+D)は多倍長整数の加算の結果であるが、ここでは"桁ごとの加算"だけを行い、"繰り上がり"の処理はツリーを用いて行う演算の最後でまとめて行う。生成した子ノードについても同じ手順を繰り返し、1桁×1桁の乗算を行う最下層のノードまで展開する。**」
   - MDの「計算結果の左右にある値」「乗算を要素として…3個のノードに分割して」は崩壊。

6. **表3の崩壊（行96〜106）— elements[] の格納順が真逆**
   - 正：
     | 名称 | 種類 | 型 | 内容 |
     |---|---|---|---|
     | elements[] | 配列 | ノード | ツリーのノードを管理する配列。**ルートノードを先頭に**、各層の左側のノードから順に要素を格納する。図2の場合は、{1234×5678, 12×56, 34×78, 46×134, 1×5, 2×6, …}の順で格納する。 |
     | layer_top[] | 配列 | 整数 | ルートノードから順に、各層の左端のノードの、elements配列上での添字の値を格納する。**図2の場合は1234×5678、12×56、1×5の添字に対応する{1, 2, 5}が入る。** |
     | mod(m, k) | 関数 | 整数 | mをkで割った剰余を整数で返す。 |
     | **new_elem(k, v1, v2)** | 関数 | ノード | **取り扱う多倍長整数の桁数がkで、v1×v2の乗算を表すノード構造体を新規に一つ作成して返す。** |
     | pow(m, k) | 関数 | 整数 | mのk乗を整数で返す。kが0の場合は1を返す。 |
     | t_depth | 変数 | 整数 | **ツリーの層の数。**図2の場合は3である。 |
     | val1, val2 | 変数 | 多倍長整数 | 乗算する対象の二つの値。図2の場合、ルートノードの二つの値で、val1は1234、val2は5678である。 |
     | **answer** | 変数 | 多倍長整数 | 乗算の計算結果を格納する変数 |
   - MDは「最下段のノードを先頭に」（真逆）、new_elem欠落、answerが「result」になっているなど崩壊。
   - 表3の導入文も復元：「…表2、表3中のp、q、v1、v2の型は多倍長整数である。また、**図3、図4中の変数は全て大域変数である。**」

## B. 中程度（本文・表の誤変換）

1. **図1（456+789）の崩壊（行24〜27）**：正は
   ```
   桁ごとの加算：{6, 5, 4} + {9, 8, 7} → {6+9, 5+8, 4+7} → {15, 13, 11}
   繰り上がり ：{15, 13, 11} → {5, 14, 11} → {5, 4, 12} → {5, 4, 2, 1}
                （1の位の繰り上がり／10の位の繰り上がり／100の位の繰り上がり）
   ```
   MDの「{5, 4} + {9, 8, 7}」等は崩壊。
2. **見出し（行14）**：「〔多倍長整数の加算〕」→「〔多倍長整数の**加減算**〕」。
3. **行16**：正「整数の**桁ごとの値**を、1の位から順に1次元配列に格納して管理する。**例えば整数123は、要素数が3の配列に{3, 2, 1}を格納して表現する。**」（MD「桁ごとの積」誤り＋例文欠落）
4. **図1後の説明欠落**：正「"桁ごとの加算"を行うと、配列の内容は{15, 13, 11}となる。1の位は15になるが、15は10×1+5なので、10の位である13に1を繰り上げて{5, 14, 11}とする。これを最上位まで繰り返す。**最上位で繰り上がりが発生する場合は、配列の要素数を増やして対応する。減算も同様に"桁ごとの減算"と"繰り下がり"との処理で計算できる。**」
5. **行10**：正「コンピュータが一度に処理できる整数の**最大桁**には…一度に扱える桁数を超える**演算**を行う一つの方法として」（MD「最大桁数は」「乗算」）。
6. **行33**：正「…カラツバ法を適用した**計算を行うことを考える**。桁数が2のべき乗でない整数や、**桁数が異なる整数同士の乗算を扱う場合は**、上位の桁を0で埋めて処理する。」（MD「上位の桁が0である整数は」誤り）
7. **表1の崩壊（行77〜82）**：正は
   - 多倍長整数：N=多倍長整数の桁数／values=**桁ごとの値を管理する1次元配列。1の位の値から順に値を格納する。配列の要素は、必要な桁を全て格納するのに十分な数が確保されているものとする。**
   - ノード：N=**ノードが取り扱う多倍長整数の桁数。図2の1234×5678のノードの場合は4である。**／val1=乗算記号の左側の値／val2=乗算記号の右側の値／**result=乗算の計算結果**（MDでresult行が欠落）。
8. **表2の崩壊（行86〜94）**：関数名は「**lradd**」（MD「iradd」）。各行の正：
   - add(p, q)：pとqについて、"桁ごとの加算"を行う。
   - carry(p)：pについて**"繰り上がり"・"繰り下がり"**の処理を行う。
   - left(p, k)：pについて、**valuesの添字が大きい方のk個の要素を返す。pのvaluesが{4, 3, 2, 1}、kが2であれば、valuesが{2, 1}の多倍長整数を返す。**
   - right(p, k)：同様に**添字が小さい方**のk個。例では{4, 3}。
   - lradd(p, k)：add(left(p, k), right(p, k))の結果を返す。
   - shift(p, k)：pを**10^k倍**する。
   - sub(p, q)：pとqについて、"桁ごとの減算"を行い**p−qを返す**。
   - 型の列は全行「多倍長整数」（戻り値）。
9. **設問3・4の解説書き直し（行231〜250）**：ウ=各層のノード数`pow(3, i−1)`を加えて次層の先頭添字を得る／エ=`3*(i−1)`（親iの子①の位置。図4のcidxと共通）／オ・カ=`pe.val1`・`pe.val2`（順不同）／キ=`mod(mul, 10)`（**1の位**）／ク=`elements[cidx＋2]`（γ）／ケ=`elements[cidx]`（α）。MDの解説文はコード崩壊に引きずられているため全面差し替え。

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 ア | 3×7 | 3×7 | ○（図2にア・イを復元、A-4） |
| 設問1 イ | 4×12 | 4×12 | ○（解説は書き直し） |
| 設問2 ① | 48 | 48 | ○（式の形式をA-3で修正） |
| 設問2 ② | 260 | 260 | ○ |
| 設問2 ③ | 48 | 48 | ○ |
| 設問2 ④ | 84 | 84 | ○ |
| 設問3 ウ | pow(3, i−1) | pow(3, i-1) | ○（出現位置をA-1で修正） |
| 設問3 エ | 3*(i−1) | 3*(i-1) | ○（同上） |
| 設問3 オ | pe.val1（順不同） | pe.val1 | ○（同上） |
| 設問3 カ | pe.val2（順不同） | pe.val2 | ○（同上） |
| 設問4 キ | mod(mul, 10) | mod(mul, 10) | ○（出現位置をA-2で修正） |
| 設問4 ク | elements[cidx＋2] | elements[cidx + 2] | ○（同上） |
| 設問4 ケ | elements[cidx] | elements[cidx] | ○（同上） |
| 設問5 | 2×N | 2×N | ○ |

## D. その他・画像確認

- 画像参照 `../images/2023春_問03_図01〜図04` の実在と内容を修正時に確認（崩壊コードの画像でないか要確認。特に図2はア・イの空欄付きであること）。
- キーワード表は概ね妥当。
