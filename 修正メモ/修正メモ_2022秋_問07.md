# 修正メモ：2022秋_問07_組込みシステム.md

照合元：`pdfs/2022r04a_ap_pm_qs.pdf` p.38〜41（問7本体）、公式解答 `pdfs/2022r04a_ap_pm_ans.pdf`
※**設問1(2)の解答誤り：正＝100,000（MDは「100,100」）。設問文も「10kHzのカウントダウンタイマー」が正（MDは「18kHz」捏造）**。下線①（10ミリ秒周期・5回連続＝設問1(1)の根拠）が別文に置換、表1タスク一覧が全面崩壊、設問2(2)本文が真逆、設問3(1)の空欄gが本文から消失。

## A. 重大（設問・解答に影響）

1. **設問1(2)の解答・設問文の誤り（行66、110〜113）**
   - 設問文 正：「ロックを解除した後の異常を**10kHz**のカウントダウンタイマーを使用して、タイマーの値が0になったときに異常と判断する。タイマーに設定する値を10進数で**求めよ**。ここで、1k＝10³とする。」（MDの「18kHz」「カウンタタイマー」は捏造）
   - 公式解答：**100,000**（MD「100,100」は誤り）。
   - 解説書き直し：10kHz＝1秒間に10,000カウント（1カウント0.1ミリ秒）。異常判断はロック解除後**10秒**（本文・表1）なので、10,000×10＝**100,000**。MDの「18,000Hz…」解説は全削除。

2. **下線①の置換（行34）— 設問1(1)(a)(b)の根拠**
   - 正：「①**制御部は、傘検知部のセンサー出力の変化を検出すると10ミリ秒周期で出力を読み出し、5回連続で同じ値が読み出されたときに、確定と判断し、その値を確定値とする**。傘の特定には、RFIDリーダーで読み出した情報（以下、RFIDタグの情報という）が使用される。傘貸出機が貸出し、返却を行うためのロックを解除した後**10秒**経過しても傘の貸出し、返却が行われなかった場合は、異常と判断し、ロックを掛ける。異常の際は、制御部がスピーカーから音声を出力して、異常が発生したことを利用者に知らせる。」
   - MDは下線①が「制御部は、傘検知部のセンサー出力の変化を検出し、異常を知らせる音声をスピーカーから出力する」に置換され、10ミリ秒周期・5回連続・10秒の記述が全て消失。設問1(1)(b)（＝10ms×4回分＝40ミリ秒）と設問1(2)（＝10秒）の根拠。

3. **設問1(1)の設問文崩壊（行62〜64）**
   - (a) 正：「制御部が**確定値を算出するのに**、複数回センサー出力を読出しする理由を20字以内で答えよ。」
   - (b) 正：「制御部が**センサー出力の変化を検出してからセンサー出力の確定ができるまで最小で何ミリ秒か**。答えは小数点以下を切り捨てて、整数で答えよ。」（MDの「ロックが解除した後の異常を検知するために…何ミリ秒待つか」は捏造）
   - (b)解説を書き直し：変化検出後、10ミリ秒周期で5回読み出す。1回目の読出しを変化検出直後とすれば、最小＝10×4＝**40ミリ秒**。

4. **表1 タスク一覧の全面崩壊（行48〜52）— 設問2・設問3の根拠**
   - 正しい表1（3タスクとも箇条書き）：
     - **メイン**：・管理サーバから指示を受信すると、貸出タスク又は返却タスクへ送信する。・"RFID情報"を受けると、RFIDタグの情報を確認し、"正常"又は"異常"を必要とする送信元タスクへ送信する。・"ロック解除完了"を受けると、傘の貸出し又は返却が可能なことを知らせる音声をスピーカーから出力する。・**"完了"を受けると、管理情報を更新し、管理サーバへ管理情報を送信する。**・"異常終了"を受けると、異常を知らせる音声をスピーカーから出力し、管理サーバに異常終了を送信する。
     - **貸出**：・要求を受けると、センサーで傘を検知し、RFIDリーダーでRFIDタグの情報を読み出し、"RFID情報"をメインタスクに送信してから、傘貸出機のロックを解除し、"ロック解除完了"をメインタスクに送信する。・傘が取り出されたことをセンサーで検知すると、傘貸出機のロックを掛け、メインタスクへ"完了"を送信する。・ロックを解除した後、10秒経過しても傘が取り出されなかった場合は、傘貸出機のロックを掛け、メインタスクへ"異常終了"を送信する。
     - **返却**：・要求を受けると、センサーで傘を検知し、RFIDリーダーでRFIDタグの情報を読み出し、"RFID情報"をメインタスクに送信する。送信後"正常"を受けると、傘貸出機のロックを解除し、"ロック解除完了"をメインタスクに送信する。・傘が傘貸出機へ返却されたことをセンサーで検知すると、傘貸出機のロックを掛け、メインタスクへ"完了"を送信する。・"異常"を受けると、傘貸出機のロックを掛け、メインタスクへ"異常終了"を送信する。・ロックを解除した後、10秒経過しても傘が返却されなかった場合は、傘貸出機のロックを掛け、メインタスクへ"異常終了"を送信する。
   - MDのメイン行は複数文が混線、返却行は設問2(2)の本文の混入（削除）。設問2(1)（a＝メイン、b＝管理サーバ）、設問3(1)（e〜h）、設問3(2)（完了→管理情報更新・送信）の全根拠。

5. **設問2(2)本文の真逆化（行76）**
   - 正：「メインタスクは、不正な傘を返却させないように、返却タスクが傘から読み出した `[c]` **に対し**、`[d]` **と異なっていないか確認し、異なっていなければ**、返却タスクに"正常"を送信する。返却タスクはメインタスクから"正常"を受けるまで、ロックを解除しない。」
   - MD「一致しているか確認し、**合致していなければ**、返却タスクに"正常"を送信」は真逆。

6. **設問3(1)本文の崩壊：空欄gが消失（行92）**
   - 正：「…"RFID情報"を送信後、傘貸出機のロックを解除し、"`[e]`"をメインタスクに送信する。**傘が傘貸出機から取り出されたことを `[f]` すると、傘貸出機の `[g]`、メインタスクへ"`[h]`"を送信する。**」
   - MDは「`[f]`することが確認できたことを、傘貸出機から取り出されたことを確認して、メインタスクへ`[h]`」と崩壊し、**空欄gが本文にない**。公式解答：e＝ロック解除完了、f＝センサーで検知、g＝ロックを掛け、h＝完了。

7. **管理情報の段落の欠落（行36）— 設問3(2)の根拠**
   - 正：「**傘貸出機は、利用者への傘の貸出し又は返却が終了すると**、自機が保有する傘の本数及び傘を識別するRFIDタグの情報（以下、これらを管理情報という）を**更新し、管理サーバに送信する**。傘貸出機は、全ての管理情報を管理サーバから受信し、記憶する。」
   - MD「管理用終了後…最新情報に更新する」は崩壊し「管理サーバに送信する」が欠落。

## B. 中程度（本文の誤変換）

1. **冒頭（行20）**：正「傘貸出機は、**スマートフォンで動作する専用のアプリケーションプログラム（以下、アプリという）**と組み合わせて傘の貸出し又は返却を行う。利用者がアプリを使って、利用する傘貸出機に貼り付けてある2次元バーコードの情報を**読み、傘貸出機を特定する**。」
2. **図3の説明（行27）**：正「ロック機構—制御部。ロック機構の下に**傘検知部**（光センサー、RFIDリーダー）。制御部の下にスピーカー。制御部が無線で管理サーバと接続。」（MD「傘収納部」誤り）
3. **ロック機構（行33）**：正「ロック機構は、**1度の操作で**傘貸出機から1本の傘の貸出し、又は、1本の傘の返却が**できる**。」
4. **傘の本数（行35）**：正「傘貸出機内の傘の本数は、制御部**で**管理する。」

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1)(a) | ノイズなどによる誤動作を防ぐため | 同一 | ○（設問文をA-3で修正） |
| 設問1(1)(b) | 40 | 同一 | ○（根拠の下線①をA-2で復元、解説書き直し） |
| 設問1(2) | 100,000 | 「100,100」 | **×要修正（A-1）** |
| 設問2(1) a | メイン | 同一 | ○ |
| 設問2(1) b | 管理サーバ | 同一 | ○ |
| 設問2(2) c | ア（RFIDタグの情報） | 同一 | ○（本文の真逆をA-5で修正） |
| 設問2(2) d | エ（貸出中の傘） | 同一 | ○ |
| 設問3(1) e | ロック解除完了 | 同一 | ○ |
| 設問3(1) f | センサーで検知 | 同一 | ○（空欄位置をA-6で復元） |
| 設問3(1) g | ロックを掛け | 「ロックをかけ」 | ○（表記を公式に合わせ、空欄gをA-6で復元） |
| 設問3(1) h | 完了 | 同一 | ○ |
| 設問3(2) | 管理情報を更新し、管理サーバへ送信する。 | 同一 | ○（根拠をA-4・A-7で復元） |

## D. その他・画像確認

- 画像参照 `../images/2022秋_問07_図01・図03・表01` の実在と内容を修正時に確認（図3はロック機構＋傘検知部＋光センサー／RFIDリーダー＋スピーカー＋制御部）。
- キーワード表は概ね妥当。
