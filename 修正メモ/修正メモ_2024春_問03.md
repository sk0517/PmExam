# 修正メモ：2024春_問03_プログラミング.md

照合元：`pdfs/2024r06h_ap_pm_qs.pdf` p.18〜22（問3本体）、公式解答 `pdfs/2024r06h_ap_pm_ans.pdf`
※解答の記号・値自体は全問公式と一致（C参照）。ただし図1・表1・図2の内容が実物と大きく異なり、解答と本文が矛盾している。

## A. 重大（解答の根拠・設問に影響）

1. **図1のグラフのエッジ距離が別物（行26〜30）— 解答ア=17と矛盾**
   - MDの記述：V2-V3=7、V3-V5=8、V4-V5=5、「最短経路距離 10+7+8=25」
   - 正しいエッジ：**V1-V2=10、V1-V3=16、V2-V4=3、V2-V3=4、V3-V4=2、V4-V5=6、V3-V5=3**
   - 正しい最短経路 V1→V2→V3→V5 ＝ 10＋4＋3＝**17**（＝空欄アの答え）。MDの「25」は矛盾で、設問1の解説（行171〜173「17+8=25」等の混乱）も全面書き直し。

2. **表1が完全に別物（行54〜62）**
   - 正しい表（列見出しも違う）：

     | 探索適用回数 | 更新起点ノード | 最短距離が確定していない、更新起点ノードに隣接するノード | 最短距離が確定していないノード |
     |---|---|---|---|
     | 1回目 | V1 \<0\> | V2 \<10\>, V3 \<16\> | V2 \<10\>, V3 \<16\>, V4 \<INF\>, V5 \<INF\> |
     | 2回目 | V2 \<10\> | V3 \<14\>, V4 \<13\> | V3 \<14\>, V4 \<13\>, V5 \<INF\> |
     | 3回目 | V4 \<13\> | V3 \<14\>, V5 \<19\> | V3 \<14\>, V5 \<19\> |
     | 4回目 | V3 \<14\> | V5 \<ア\> | V5 \<ア\> |
     | 5回目 | V5 \<ア\> | − | − |

   - 注記1：INFは、定数で十分大きい数を表す。注記2：\<\>内の数値は、当該ノードの始点ノード距離を表す。
   - ア＝V3\<14\>経由で 14＋3＝**17**。MDの表は行数・列・値すべて誤り（V3\<17\>など）。

3. **図2プログラムの空欄配置・宣言が誤り（行74〜104）**
   - 正しいプログラム（29行）：
     ```
     1:  ○整数型: distance()
     2:  整数型: N            /* ノードの個数 */
     3:  整数型: INF          /* 十分大きい定数 */
     4:  整数型の二次元配列: edge  /* edge[m, n]には、ノードmからノードnへの距離を格納
                                    二つのノードが隣接していない場合にはINFを格納 */
     5:  整数型: GOAL         /* 終点のノード番号 */
     6:  整数型の配列: dist   /* 始点ノード距離。初期値はINF。要素番号がノード番号を表す。 */
     7:  整数型の配列: done   /* 初期値は0。最短距離が確定したら1を入れる。要素番号がノード番号を表す。 */
     8:  整数型: curNode      /* 更新起点ノードのノード番号 */
     9:  整数型: minDist      /* 更新起点ノードを求める際に使用する一時変数 */
     10: 整数型: k            /* 要素番号 */
     11: dist[1] ← 0
     12: while (true)
     13:   minDist ← INF
     14:   for (kを1からNまで1ずつ増やす)
     15:     if (done[k]が0 かつ [ イ ])
     16:       minDist ← [ ウ ]
     17:       curNode ← k
     18:     endif
     19:   endfor
     20:   done[curNode] ← 1
     21:   if (curNodeがGOALと等しい)
     22:     return dist[curNode]
     23:   endif
     24:   for (kを1からNまで1ずつ増やす)
     25:     if ([ エ ] が dist[k] より小さい かつ done[k]が0)
     26:       dist[k] ← [ エ ]
     27:     endif
     28:   endfor
     29: endwhile
     ```
   - MDとの主な差分：
     - **GOALの宣言（行5）とkの宣言（行10）が欠落**
     - **空欄ウの位置**：正しくは行16「minDist ← ウ」。MDは行16に`dist[k]`を直書きし、行25に架空の空欄ウを置いている
     - **行25は空欄エ**（「エ が dist[k]より小さい」）。空欄エは行25と行26の2か所に登場する
     - doneは**整数型**（0/1）。MDの「論理型」「真/偽」は誤り。行15の条件順も「done[k]が0 かつ イ」、行20は「done[curNode] ← 1」
     - edge のコメント：「edge[m, n]には、ノードmからノードnへの距離を格納」（MD「原始辺」は誤変換）
   - 設問2の解答（イ=dist[k]がminDistより小さい／ウ=dist[k]／エ=dist[curNode]＋edge[curNode, k]）は公式一致だが、解説の行番号対応を上記の正しい配置で書き直す。

4. **〔最短経路の出力〕の挿入位置の記述が崩壊（行110）— 設問3(1)の前提**
   - 正：「具体的には、まず、ノード番号1〜Nを格納する配列viaNodeを使用するために、**図3の変数宣言を図2の行10の直後に、図4のプログラムを図2の行21の直後に、それぞれ挿入する**。さらに、各ノードの始点ノード距離を更新するたびに、直前に経由したノード番号をviaNodeに格納する**①代入文を一つ、図2のプログラムの行 `[オ]` の直後に挿入する**。」
   - MDの「図3の変数宣言書の2行目の直後に…」は架空。「行 オ の直後に挿入する」が本文から分離して図4後の引用ブロック（行130）に置かれているのも直す。
   - オ＝**25**（行26ではなく…公式解答は25。dist[k]更新のif文内、行25の直後＝行26の位置に挿入だが「最も小さい番号」の指定により25）※解説では「dist[k]←エ（行26）と同じif文内で、行25の直後」の趣旨で説明。公式：代入文=viaNode[k] ← curNode、オ=25。

5. **〔計算量の考察〕の本文が丸ごと欠落 — 設問4の対象セクション**
   - 図4の後に復元：「関数distanceでは、次の `[キ]` を選ぶために始点ノード距離を計算する回数は最大でもN回である。また、`[キ]` を選ぶ回数は、一度選ばれると当該ノードの最短距離は確定するので、最大でもN回である。よって、最悪の場合の計算量は、O(`[ク]`) である。」
   - キ=更新起点ノード、ク=N²。MDは本文なしで設問だけ存在。

6. **〔始点から終点までの最短距離を求める手順〕の①が崩壊（行38〜46）**
   - 正：「最初に、各ノードについて、始点からそのノードまでの距離（以下、始点ノード距離という）を作業用に導入して**十分に大きい定数としておく。ただし、始点の始点ノード距離は0とする。** この時点では、どのノードの最短距離も確定していない。**次に、終点の最短距離が確定するまで、①〜③を繰り返す。ここで、始点との距離を算出する基準となるノードを更新起点ノードという。**」
   - ①正：「**最短距離が確定していないノードの中で、始点ノード距離が最小のノードを更新起点ノードとして選び、そのときの始点ノード距離の値で、当該更新起点ノードの最短距離を確定する。** 更新起点ノードを選ぶ際に、始点ノード距離が最小となるノードが複数ある場合は、その中の任意のノードを更新起点ノードとして選ぶ。」
   - ③正：「①で選択した更新起点ノードに隣接しており、かつ、最短距離が確定していない全てのノードについて、**更新起点ノードを経由した場合の始点ノード距離を計算する**。ここで計算した始点ノード距離が、そのノードの現在までの始点ノード距離よりも小さい場合には、そのノードの現在までの始点ノード距離を更新する。」
   - MD行46「①～③の処理を繰り返す。」は不要（繰返し条件は導入文にある）。

## B. 中程度（本文の誤変換）

1. 行16：「エッジをどちらの方向にも行き来することができ**、任意の二つのノード間に経路が存在するグラフを扱う**。ここで、グラフを次のように**定義**する。」（MDは文が切れて「考える」）
2. 行19：正：「二つのノードが他のノードを経由せずにエッジでつながっているとき、**それらのノードは隣接するという。隣接するノード間のエッジには、ノード間の距離として正の数値を付ける。**」（MDは「そのエッジの距離は隣接するノード間の距離という」と意味不明）
3. 行20：「距離の和が最小の**距離を最短距離**という。始点から終点まで**の最短距離となる経路**を最短経路という。」
4. 行22：「V1, V2, V3, V5のノードを**順に**たどる経路である。」
5. 図4（行121〜128）：「GOAL**を**出力」「endwhile**(is)**」→「endwhile」。コメント位置：「/* 最短経路の出力 */」はwhile行に付く。
6. 設問3(1)（行148）：「本文中の下線①と `[オ]` について、挿入すべき代入文と `[オ]` に入れる行の番号を答えよ。」（MDはおおむね一致、「ただし、図2中の現在の行の番号は…」の「図2中の」を補う）

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 ア | 17 | 17 | ○（ただし解説が図1誤記のため混乱→書き直し） |
| 設問2 イ | dist[k]がminDistより小さい | 同一 | ○ |
| 設問2 ウ | dist[k] | dist[k] | ○（空欄位置はA-3のとおり要修正） |
| 設問2 エ | dist[curNode]＋edge[curNode, k] | 同一 | ○ |
| 設問3(1) | viaNode[k] ← curNode ／ オ=25 | 同一 | ○ |
| 設問3(2) カ | ア | ア | ○ |
| 設問4(1) キ | 更新起点ノード | 更新起点ノード | ○（本文セクションがA-5のとおり欠落） |
| 設問4(2) ク | N² | N² | ○（同上） |

## D. その他・画像確認

- 画像参照 `../images/2024春_問03_図01_グラフ例.png`、`表01_最短距離推移.png`、`図02_distanceプログラム.png` の実在と内容（正しい値の図か）を修正時に確認。
- キーワード表（行219〜）は妥当。
