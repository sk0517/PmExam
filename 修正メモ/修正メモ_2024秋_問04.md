# 修正メモ：2024秋_問04_システムアーキテクチャ.md

照合元：`pdfs/2024r06a_ap_pm_qs.pdf` p.18〜22（問4本体）、公式解答 `pdfs/2024r06a_ap_pm_ans.pdf`
※本文・表・設問とも崩壊が広範囲。表1・表4・空欄e・設問2(3)(4)・設問3が欠落または架空。ほぼ全面書き直しが必要。

## A. 重大（解答・設問に影響）

1. **表1（新システムに必要な機能一覧）が丸ごと欠落 — MDの〔新システムに必要な機能〕の箇条書きは架空**
   - 正しいセクション：「D君は、現在の動画配信サービスの機能を基に、新システムに必要な動画配信者向け機能の機能要件を定義した。新システムに必要な機能一覧（抜粋）を表1に示す。」
   - 表1：
     | 機能名 | 機能概要 |
     |---|---|
     | アップロード | 動画配信者がビデオカメラやスマートフォンで撮影した動画をC社のWebサイトにアップロードして登録する機能 |
     | 動画編集 | C社のWebサイトに登録された動画を、Webブラウザを用いて編集する（カットする、結合する、BGM・テロップを挿入するなど）機能 |
     | 配信準備 | 動画視聴者のネットワーク環境に合わせた複数のビットレートの動画ファイルを作成する機能 |
     | アクセス分析 | 動画が動画視聴者に何回アクセスされているかを、複数の分析軸（地域、日時、年齢層、端末種別、アクセス動線など）から分析する機能 |
   - MD行18〜23の箇条書き（「レンダリング処理：動画データのフレームを生成する処理」等）は削除。

2. **〔新システムのサーバ構成〕セクション本文が欠落 — 下線①・空欄a・性能値**
   - 復元：「D君は、新システムのサーバ構成を設計した。…SSDとHDDのアクセス速度を表2に、LANとインターネットの通信速度を表3に、**各サーバのCPUとGPUの搭載数を表4**に示す。なお、新システム内の負荷分散装置、サーバ、NASはLANで接続されている。」
   - 続けて：「各サーバで計算を行うCPUやGPUで実行される演算は、**①整数演算**と浮動小数点演算の二つに分類される。新システムで利用するCPUは、**1コア当たり整数演算が10,000G Operations／秒（以下、OPSという）、浮動小数点演算が500G Floating-point Operations／秒（以下、FLOPSという）**で実行できる。また、**GPUは1個当たり浮動小数点演算が10,000G FLOPSで実行できる**。」
   - 空欄a文：「この新システムを用いて、**APサーバ1がAPサーバ1のSSDに格納された800Mバイトの動画ファイルをメモリに読み込む時間は `[a]` 秒**である。また、DBサーバのDBMSはRDBである。」（a=800÷500=1.6）
   - MDの見出し「〔処理時間の算出〕」とその方針箇条書きは架空→削除し、正しい見出し「〔配置の検討〕」に。

3. **表4（各サーバのCPUとGPUの搭載数）が丸ごと欠落 — b・cの計算根拠**
   | サーバ | CPU（コア） | GPU（個） |
   |---|---|---|
   | APサーバ | 16 | 0 |
   | DBサーバ | 32 | 0 |
   | GPUサーバ | 4 | 4 |

4. **表2・表3の数値誤り**
   - 表2：SSD書込速度=**400**（MD「500」）。表の項目名は「装置」。
   - 表3：**LAN=1Gbps**（MDは空欄）、インターネット=10Gbps。注記「表3中のbpsはビット／秒を指す。」

5. **表5が崩壊（行52〜57）**
   - 正しい内容（列：機能名／処理名／処理方法／インプット(Mバイト)／アウトプット(Mバイト)／整数演算数(GO)／浮動小数点演算数(GFLO)）：
     - 動画編集｜素材送信｜NASに格納されているBGM素材やテロップ素材を、NASからAPサーバへ送信し、APサーバから動画配信者のPCへ送信する。｜50｜50｜10｜0
     - 配信準備｜レンダリング｜APサーバのメモリに格納されている動画データを基にBGM、テロップなどを合成して一つの動画データを作成する。｜500｜100｜0｜**100,000**
     - アクセス分析｜オンラインユーザ数取得｜DBサーバに格納されている特定の動画を視聴しているオンラインユーザ数をリアルタイムに集計して表示する。｜100｜0.1｜50｜0
     - アクセス分析｜多次元分析｜DBサーバに格納されている前日までの動画の視聴ログを複数の分析軸から分析する。｜50,000｜1｜300｜0
   - 注記：「GOはGiga Operationsを示し、GFLOはGiga Floating-point Operationsを示す。」

6. **〔配置の検討〕の本文崩壊 — 空欄eが欠落（行66〜68）**
   - 正：「レンダリング処理は、APサーバのCPUを**4コア**用いて処理する場合には `[b]` 秒掛かり、GPUサーバのGPUだけを1個用いて処理する場合には `[c]` 秒掛かる。ただし、GPUサーバを用いて処理する場合には、APサーバからGPUサーバへデータを送信するのに `[d]` 秒掛かり、**APサーバへレンダリング結果を返信するのに `[e]` 秒掛かる**。この検討結果から、APサーバよりもGPUサーバで処理した方がよい。」
   - 計算：b=100,000GFLO÷(500G×4コア)=**50**、c=100,000÷10,000=**10**、d=500Mバイト÷125Mバイト/秒(=1Gbps)=**4**、e=100÷125=**0.8**
   - 下線②文 正：「オンラインユーザ数取得処理について、D君は**②DBサーバで処理した結果をAPサーバへ送信する方法が最適と考え**、DBサーバで処理することにした。」（MD「208 DBサーバ」は誤変換）
   - 素材送信・下線③ 正：「素材送信処理は、同時に100名の動画配信者向けに素材データを送信しようとすると、**LANの通信速度とHDDの読込速度が遅く**送信に時間が掛かる。しかし、**図1のサーバ構成の変更には大きな費用が掛かることから、サーバ構成を変更せずに対応する方法を検討した。動画配信者のPCへ送信する素材データは特定の素材データに偏っており、各素材データの更新頻度も高くないことから、表5の③素材送信処理の処理方法を変更して対応することにした。**」（MD「SSDを用いて対応することとした」は解答の漏えい的な崩壊文）

7. **〔動画配信者数増大への対応方針〕の崩壊（行74）**
   - 正：「…まず、表5のレンダリング処理は、動画データごとに処理が独立しており、**GPUサーバを手動で追加する `[f]` の対応**を行う方針にした。一方で、オンラインユーザ数取得処理は、時間とともに断続的に追記される動画の視聴ログをリアルタイムに集計する処理であり、**DBサーバの数を増やせないことから、DBサーバを `[g]` して対応を行う方針にした**。また、多次元分析処理は、**④そこで扱うデータの特徴**から、多次元分析処理専用のDBサーバを追加する方針にした。これによって多次元分析処理の負荷が他の処理へ影響しないようになる。」

8. **設問の構成が崩壊 — 設問2(3)(4)・設問3が欠落／架空**
   - 正しい設問構成：
     - 設問1(1) 下線①：整数演算を**全て**選ぶ。解答群は**ア〜カの6択**（MDは「カ 3.00×0」が欠落）：ア 300+200−100／イ 300×200÷100／ウ 3.00+2.00−0.10／エ −300+(−200)−(−100)／オ 300×0／**カ 3.00×0**
     - 設問1(2) 空欄a（読込速度・書込速度を実効速度とし、他のオーバヘッドは無視。小数第2位を四捨五入して小数第1位まで）
     - 設問2 前文：「〔配置の検討〕について答えよ。ここで、**図1のサーバ構成を一つの処理が占有でき、〔新システムのサーバ構成〕と表5に記載以外のオーバヘッドや並列処理に伴うオーバヘッドは無視できるものとする**。」
     - 設問2(1) 空欄b〜d（**整数で**答えよ）
     - 設問2(2) 空欄e（小数第2位を四捨五入して小数第1位まで）
     - 設問2(3) 下線②：「DBサーバで処理した結果をAPサーバへ送信する方法が最適と考えたのはなぜか。**データ量の観点から35字以内**で答えよ。」
     - 設問2(4) 下線③：「素材送信処理の処理方法をどのように変更したか。**変更点を30字以内**で答えよ。」
     - 設問3(1) 空欄f・g：解答群「ア スケールアウト／イ スケールアップ／ウ スケールイン／エ スケールダウン」
     - 設問3(2) 下線④：「オンラインユーザ数取得処理と対比して、多次元分析処理で扱うデータの特徴を**20字以内**で答えよ。」

## B. 中程度（本文の誤変換）

1. 行10：「Webブラウザを用いて…C社のWebサイトにアップロードすると、Webサイト上で動画の編集・配信、**広報**、アクセス分析などの機能が利用できる。」（MD「アップロードすとYと」「WebサイトJE」崩壊。※原文は「広報」）
2. 図1の説明（行30〜32）修正：PC（複数）→インターネット→負荷分散装置→APサーバ1・APサーバ2（各CPU/SSD）→DBサーバ（CPU/SSD）・GPUサーバ（CPU/GPU/SSD）・NAS（HDD）。GPUサーバにはCPUも搭載。NASはHDDのみ。注記「図1中のAPサーバはアプリケーションサーバを示す。また、DBサーバはデータベースサーバを示す。」

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1) | ア、イ、エ、オ | 同一 | ○（解答群にカを追加） |
| 設問1(2) a | 1.6 | 1.6 | ○（計算根拠を「800Mバイト÷SSD読込500Mバイト/秒」に書き直し） |
| 設問2(1) b | 50 | 50 | ○（根拠：100,000GFLO÷(500G FLOPS×4コア)） |
| 設問2(1) c | 10 | 10 | ○（根拠：100,000÷10,000） |
| 設問2(1) d | 4 | 4 | ○（根拠：500Mバイト÷125Mバイト/秒） |
| 設問2(2) e | 0.8 | **欠落** | **×（設問・本文とも復元）** |
| 設問2(3) | アウトプットデータと比較してインプットデータの量が多いから | 「設問2(2)」として掲載（設問文は架空） | **△（設問文をA-8のとおり修正）** |
| 設問2(4) | 素材データをAPサーバのSSDにキャッシュする。 | 解説内に断片あり | **×（設問として復元）** |
| 設問3(1) f | ア（スケールアウト） | **欠落** | **×（復元）** |
| 設問3(1) g | イ（スケールアップ） | **欠落** | **×（復元）** |
| 設問3(2) | 追加・更新のない過去のデータ | 「設問2(3)」として掲載 | **△（設問番号・設問文を修正）** |

## D. その他・画像確認

- 画像参照 `../images/2024秋_問04_図01_サーバ構成.png`、`表05_処理一覧.png` の実在と内容を修正時に確認。表1・表4の画像がなければ追加検討。
- キーワード表に「スケールアウト／スケールアップ」を追加。
