# 修正メモ：2022秋_問06_データベース.md

照合元：`pdfs/2022r04a_ap_pm_qs.pdf` p.32〜36（問6本体）、公式解答 `pdfs/2022r04a_ap_pm_ans.pdf`
※最終解答記号は概ね一致するが、**根拠となる本文・表がほぼ全面崩壊**。表2ヒアリング結果が全面捏造、図1から**請求エンティティ（空欄aの位置）が丸ごと欠落**、表3の空欄g〜iの位置が崩壊（正: g・h・iは表3・表4とも**項番1の主キー行のPK/UK/非NULL欄**）、図3のSQLの空欄k・l・mの位置ずれ。設問1のb・e・fの解説が誤り（b=1対多であって1対1ではない等）。

## A. 重大（設問・解答に影響）

1. **表2 ヒアリング結果の全面捏造（行50〜55）— 設問1（a・請求先部署ID）・設問3（j）の根拠**
   - 正しい表2は「項番／課題／要件」の3列。全4行を差し替え：
     | 項番 | 課題 | 要件 |
     |---|---|---|
     | 1 | 利用者が情報端末ごとに通信事業者や料金プランを選択できるので、結果として高い料金プランを契約して利用しているケースがある。 | 通信事業者を原則としてL社に統一し、かつ、より低価格の料金プランで契約できるようにする。 |
     | 2 | 情報端末に関する費用は本社の総務部で一括して負担しており、利用者のコスト意識が低く、利用状況次第で高額な請求が発生するケースがある。 | 従業員の異動情報に基づいて請求を**年月ごと、部署ごと**に管理できるようにする。 |
     | 3 | 情報端末に対しては利用可能な機能やアプリケーションプログラム（以下、アプリという）に制限を設けており、利用者から機能制限解除の依頼やアプリ追加の依頼があっても、管理が煩雑となるので認められない状況である。 | 業務上必要な機能やアプリについては、利用者に使用目的を確認し、**従業員と情報端末の組合せごとに個別に許可**できる仕組みにする。 |
     | 4 | 契約ごとに異なる暗証番号を設定することで利用者による不正な契約変更の防止を図っているが、運用管理担当者は全ての契約の暗証番号を自由に参照できてしまうので、運用管理担当者による不正な契約変更が発生するリスクが残っている。 | 暗証番号は運用管理担当者の上長（以下、上長という）しか参照できないようにアクセスを制御する。運用管理担当者は契約変更が必要な都度、上長に申請し、上長が契約変更を行う仕組みにする。 |
   - 項番2の「年月ごと、部署ごと」が空欄a（年月）・請求先部署IDの根拠、項番4が設問3のj（GRANT SELECT (契約ID, 暗証番号)）の根拠。

2. **図1 E-R図の崩壊：請求エンティティ欠落ほか（行67〜79）— 設問1全体の根拠**
   - 正しいエンティティと属性（下線=主キー、破線下線=外部キー）：
     - **請求**（<u>請求番号</u>, 契約ID(FK), **`[a]`**, 請求先部署ID(FK), 基本料金, 通話料金, 通信料金, その他料金, 請求金額合計）← **MDから丸ごと欠落。空欄aはここ**
     - **情報端末**（<u>情報端末ID</u>, 契約ID(FK), 機種名, 交換予定日, 廃棄日）（MDの「機能名」誤り・契約ID欠落）
     - **契約**（<u>契約ID</u>, 料金プランコード(FK), 回線番号, **内線電話番号**, 暗証番号）（MDの「確認番号」「内線番号情報番号」は捏造）
     - **機能制限解除**（<u>機能制限解除番号</u>, 機能制限ID(FK), 利用ID(FK), 使用目的）（MDの「機能制限設定」「利用可否」は誤り）
     - **機能制限**（<u>機能制限ID</u>, 機能制限名, 制限内容）（MDの「利用禁止日」は捏造）
     - **利用**（<u>利用ID</u>, **`[c]`**, **`[d]`**, 利用開始日, 利用終了日）← **空欄c・dはここ（MDは位置記載なし）**
     - **従業員**（<u>従業員ID</u>, 所属部署ID(FK), 氏名, メールアドレス）
     - **部署**（<u>部署ID</u>, 部署名, 上位部署ID(FK)）
     - **料金プラン**（<u>料金プランコード</u>, 通信事業者コード(FK), 料金プラン名, 基本料金, 通話単価, 通信単価）（MDは通信事業者コード欠落）
     - **アプリ追加**（<u>アプリ追加番号</u>, アプリID(FK), 利用ID(FK), 使用目的）（MDの「アプリ名, 利用日, 使用料」は捏造）
     - **アプリ**（<u>アプリID</u>, アプリ名, アプリ提供会社）← **MDから欠落**
     - **通信事業者**（<u>通信事業者コード</u>, 通信事業者名）
   - 関連（空欄の位置）：**b＝情報端末—契約間**、**e＝利用—アプリ追加間**、**f＝部署—請求間**。ほかに 契約→請求（1対多）、料金プラン→契約（1対多）、通信事業者→料金プラン（1対多）、従業員→利用（1対多）、利用→機能制限解除（1対多）、機能制限→機能制限解除（1対多）、アプリ→アプリ追加（1対多）、部署→従業員（1対多）、部署の自己参照（上位部署ID）。
   - 注記の復元：「属性名の実線の下線は主キー、破線の下線は外部キーを示す。主キーの実線が付いている属性名には、外部キーの破線を付けない。」

3. **設問1の解説誤り（行172〜181）**
   - 公式解答：a＝年月、b＝↑、c＝従業員ID、d＝情報端末ID（c・d順不同）、e＝↓、f＝←。記号自体はMDと一致するが解説が誤り：
     - **b**：MD「↑（1対1）（1台の端末に1つの契約）」は誤り。正＝**契約→情報端末の1対多（矢印は情報端末向き=↑）**。同一の回線番号（契約）のままで2年ごとに旧端末から新端末へ交換するため、1契約に複数の情報端末が対応する（表1「交換予定日」の説明が根拠）。
     - **e**：MD「部署と従業員の関係」は誤り。正＝**利用→アプリ追加の1対多（↓）**。1つの利用（従業員と情報端末の組合せ）に複数のアプリ追加が対応。
     - **f**：MD「←（自己参照）部署の上位部署」は誤り。自己参照の関連は上位部署IDとして既に図中にある。正＝**部署→請求の1対多（矢印は請求向き=←）**。請求は部署ごとに管理する（表2項番2、請求先部署IDが根拠）。
     - **a**：正＝請求エンティティの属性「年月」（請求を年月ごとに管理するため）。MDの「利用状況をまとめる月次集計」はずれ。

4. **表3の空欄g〜i位置の崩壊（行91〜97）— 設問2の根拠**
   - 正しい表3：**項番1「契約ID」CHAR(8) の PK＝`[g]`、UK＝`[h]`、非NULL＝`[i]`**。
     | 項番 | 列名 | データ型 | PK | UK | 非NULL | 初期値 | アクセス制御 | その他の指定内容 |
     |---|---|---|---|---|---|---|---|---|
     | 1 | 契約ID | CHAR(8) | `[g]` | `[h]` | `[i]` | | 上長（ユーザーアカウント名：ADMIN）による参照が必要 | （省略） |
     | 2 | 料金プランコード | CHAR(8) | N | N | Y | | | 料金プラン表への外部キー |
     | 3 | 回線番号 | CHAR(13) | N | N | Y | | | （省略） |
     | 4 | **内線電話番号** | CHAR(11) | N | N | **N** | **NULL** | | （省略） |
     | 5 | 暗証番号 | CHAR(4) | N | N | Y | | **上長（ユーザーアカウント名：ADMIN）による参照が必要** | （省略） |
   - MDは列名「UX」誤記、項番4が「確認番号」でg・hを置き、iを暗証番号に置くなど全面崩壊。暗証番号行の「SELECT（契約ID, 暗証番号）とする」は解答jの混入（削除）。
   - **表4も項番1「料金プランコード」の PK/UK/非NULL が `[g]`/`[h]`/`[i]`**（MDは□のまま）。料金プラン名は **VARCHAR(30)**（MD「VARCHAR(38)」）。通信事業者コード行のその他＝「通信事業者表への外部キー。行挿入時に、初期値としてL社の通信事業者コード'1234'を設定する。」初期値欄＝1234。

5. **表3・表4の前文欠落（行85の後）— 設問2の直接の根拠**
   - 復元：「表3及び表4のデータ型欄には、適切なデータ型、適切な長さ、精度、位取りを記入する。PK欄は主キー制約、UK欄はUNIQUE制約、非NULL欄は非NULL制約の指定をするかどうかを記入する。指定する場合にはYを、指定しない場合にはNを記入する。**ただし、主キーに対してはUNIQUE制約を指定せず、非NULL制約は指定するものとする。**」
   - これが設問2＝オ（g=Y、h=N、i=Y）の根拠。

6. **設問2の解説の全面書き直し（行187〜191）**
   - 公式解答＝オ（MD一致）だが、MDの解説（確認番号のUNIQUE等）は捏造。正：g〜iは主キー（契約ID／料金プランコード）の行なので、PK＝Y、UNIQUE制約は指定しない（h＝N）、非NULL制約は指定する（i＝Y）。

7. **図3 SQLの空欄位置ずれ（行130〜141）— 設問3 k・l・mの根拠**
   - 正しい図3：
     ```
     CREATE TABLE 料金プラン
     (料金プランコード CHAR(8) NOT NULL,
      通信事業者コード [　k　],
      料金プラン名 VARCHAR(30) NOT NULL,
      基本料金 DECIMAL(5,0) NOT NULL,
      通話単価 DECIMAL(5,2) NOT NULL,
      通信単価 DECIMAL(5,4) NOT NULL,
      [　l　] (料金プランコード),
      [　m　] (通信事業者コード) REFERENCES 通信事業者(通信事業者コード))
     ```
   - MDは表名「料金プラン表」（正=料金プラン）、kを列名位置・lをCHAR(4)の後ろに置き、mをPRIMARY KEY行に置いて**FOREIGN KEY行の空欄が消失**。VARCHAR(38)も誤り。REFERENCES先は「通信事業者」（「通信事業者表」ではない）。
   - 公式解答：**k＝CHAR(4) DEFAULT '1234' NOT NULL、l＝PRIMARY KEY、m＝FOREIGN KEY**。MDの解説表（行199〜204：k=通信事業者コード、l=CHAR(4) DEFAULT '1234'、m=PRIMARY KEY）は空欄ずれに引きずられた誤りで、直後の「IPA公式」ブロックと矛盾している。解説表を公式に合わせて書き直し。

8. **表1の項目崩壊（行24〜38）— 空欄b・iの根拠を含む**
   - 「確認番号」→正しくは**内線電話番号**（内線通話に使用する番号）。「契約プラン名」→**料金プラン名**。
   - **「暗証番号」の行がMDから欠落**（説明：契約変更の手続に必要な番号。表2項番4・設問3 jの背景）。
   - 利用開始日 説明 正：「運用管理担当者が利用者に対して情報端末を貸し出した日」（MDの（上長）（ユーザ）は捏造）。
   - 利用終了日 正：「**利用者から運用管理担当者に対して**情報端末を返却した日」記入例＝2022-09-10。
   - 交換予定日 正：「J社では情報セキュリティ対策の観点から**同一の回線番号のままで2年ごとに**旧情報端末から新情報端末への交換を行っており、新情報端末に交換する予定の日」記入例＝2022-09-10（**空欄b＝1対多の根拠**。MD「2〜3年ごとに一回」は誤り）。
   - 「廃日」→**廃棄日**、記入例＝2022-10-20。情報端末IDの記入例はMD「GET78890A42」（原文の英数字列と不一致の可能性が高い。画像で要確認）。

## B. 中程度（本文の誤変換）

1. **冒頭（行10〜12）**：「情報端末のグループ全従業員への配布対象をグループ企業も含む全従業員に拡大」等の重複・崩壊を整理。「計算ソフトでスマートデバイス管理（以下、管理台帳という）を作成」→正しくは**表計算ソフトで管理台帳を作成**（「管理台帳」はスマートデバイス管理の台帳の名称）。「貸与台数が3万人を超える」→**3万台**規模。
2. **E-R図節の導入（行61）**：正「なお、J社内の部署の階層構造は、**自己参照の関連**を用いて表現する。」（MD「関係」）
3. **〔表の作成とアクセス制御〕（行116）**：正「…作成した。表3のアクセス制御を設定するためのSQL文を図2に、表4の料金プラン表を作成するためのSQL文を図3に示す。**なお、運用管理担当者のユーザーアカウントに対しては適切なアクセス制御が設定されているものとする。**」（最後の文がMDで欠落。設問3 jの前提）
4. **節見出し（行42）**：正「〔現在の管理**方法における**課題と新システムに対する要件〕」

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 a | 年月 | 同一 | ○（請求エンティティをA-2で復元、解説修正） |
| 設問1 b | ↑（1対多） | 「↑」だが解説「1対1」 | ○（**解説をA-3で修正**） |
| 設問1 c | 従業員ID（dと順不同） | 同一 | ○（空欄位置をA-2で復元） |
| 設問1 d | 情報端末ID | 同一 | ○ |
| 設問1 e | ↓（1対多） | 「↓」だが解説「部署と従業員」 | ○（**解説をA-3で修正**） |
| 設問1 f | ←（部署→請求の1対多） | 「←」だが解説「自己参照」 | ○（**解説をA-3で修正**） |
| 設問2 | オ（g=Y、h=N、i=Y） | 「オ」 | ○（**空欄位置A-4・前文A-5・解説A-6を修正**） |
| 設問3 j | SELECT (契約ID, 暗証番号) | 同一 | ○ |
| 設問3 k | CHAR(4) DEFAULT '1234' NOT NULL | 解説表「通信事業者コード」 | **×要修正（A-7、IPA公式ブロックは正しい）** |
| 設問3 l | PRIMARY KEY | 解説表「CHAR(4) DEFAULT '1234' NOT NULL」 | **×要修正（A-7）** |
| 設問3 m | FOREIGN KEY | 解説表「PRIMARY KEY」 | **×要修正（A-7）** |

※設問1 fは公式解答PDFのテキスト抽出では空欄（矢印グリフ）。図1の実物（部署→請求の関連、矢印は請求側）で確認済み。

## D. その他・画像確認

- 画像参照 `../images/2022秋_問06_表01〜04・図01〜03` の実在と内容を修正時に確認（図1に請求・アプリのエンティティがあること、表3・表4の項番1にg〜iがあること、図3のl・m行）。
- キーワード表は概ね妥当。
