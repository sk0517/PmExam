# 修正メモ：2023春_問05_ネットワーク.md

照合元：`pdfs/2023r05h_ap_pm_qs.pdf` p.28〜31（問5本体）、公式解答 `pdfs/2023r05h_ap_pm_ans.pdf`
※解答は全問公式と一致。ただし**表1のFQDN・IPアドレスの誤記が多数（設問2の解答根拠）、下線番号が全面的にずれ（正は①〜⑤）、公開ドメインの記述が誤り**など、根拠情報の崩壊が重大。

## A. 重大（解答の根拠・設問に影響）

1. **表1（DNSサーバbのAレコード）の誤記（行32〜37）— 設問2(1)(2)の直接根拠**
   - 正：
     | 項番 | 機器名称 | サーバのFQDN | IPアドレス |
     |---|---|---|---|
     | 1 | DNSサーバb | **nsb.example.jp** | 200.a.b.1/28 |
     | 2 | WebAPサーバb | miap.example.jp | 200.a.b.2/28 |
     | 3 | **DNSサーバb** | **nsb.f-sha.example.lan** | **192.168.0.1/24** |
     | 4 | WebAPサーバb | apb.f-sha.example.lan | **192.168.0.2/24** |
   - MDの誤り：項番1「nsc.example.jp」→nsb／項番3の機器名「WebAPサーバb」→DNSサーバb、FQDN「nsc.f-sha.example.jp」→nsb.f-sha.example.lan、IP「192.168.1.24」→192.168.0.1/24／項番4「192.168.8.2/24」→192.168.0.2/24。DMZは**192.168.0.0/24**。
   - 注記2 正：「各リソースレコードのTTL（Time To Live）は、**604800**が設定されている。」

2. **下線番号の全面ずれ — 正は①〜⑤**
   - 正：①=デフォルトルート／**②=優先DNSサーバとして、FQDNがnsb.f-sha.example.lanのDNSサーバbが登録されている**／③=広域イーサ網経由でWebAPサーバbにログインしてCPU使用率を調べた／④=メンテナンス中は、一つのWebサイトでサービスを提供することになるので、Mシステムを利用する顧客への影響は避けられない／⑤=手順(ii)でAレコードを無効化したWebAPサーバの状態を確認。
   - MDは②を2箇所で使い回し、④⑤を③④と誤記。設問1(2)=下線②、設問2(2)=**下線③**、設問3(2)=**下線④**、設問3(3)=**下線⑤**に修正。

3. **運用PCのDNS設定の箇条書き崩壊（行50）— 設問1(2)・空欄bの根拠**
   - 正：「運用PCには、<u>②優先DNSサーバとして、FQDNがnsb.f-sha.example.lanのDNSサーバbが登録されている</u>。」
   - MDの「DNSサーバとして、b-DCのWebサイトbへの②デフォルトルート設定の下で、接続しているDNSサーバが設定されている」は崩壊。

4. **公開ドメインの記述誤り（行48）— 空欄a（miap.example.jp）の根拠**
   - 正：「DNSサーバbは、**インターネットに公開するドメインexample.jpと、F社の社内向けのドメインf-sha.example.lan**の二つのドメインのゾーン情報を管理する。」
   - MDの「インターネットに公開するドメインであるf-sha.example.lan及びf-sha.example.jp」は誤り（公開=example.jp、社内向け=f-sha.example.lan。顧客と同じURL＝公開ドメインのmiap.example.jpの根拠）。

5. **表2（追加Aレコード）の誤記（行95〜100）**
   - 正：項番1「nsc.example.jp／**200.c.d.81/28**」、項番2「miap.example.jp／**200.c.d.82/28**」、項番3「DNSサーバc／nsc.f-sha.example.lan／192.168.1.1/24」、項番4「WebAPサーバc／apc.f-sha.example.lan／192.168.1.2/24」。
   - MDの「200.c.d1/28」「200.c.d.62/28」は誤り。注記 正：「各リソースレコードのTTLは、表1と同じ604800を設定する。」

6. **ゾーン転送の段落の分断・崩壊（行104・110）**
   - 正（一続きの文）：「表2の情報を追加登録することによって、WebAPサーバb、cが同じ割合で利用されるようになる。**DNSサーバb、cには `[c]` 転送の設定を行い、DNSサーバbの情報を更新すると、その内容がDNSサーバcにコピーされるようにする。**」
   - MDは「ゾーン情報をDNSサーバcに複製される」と崩し、さらに「情報を更新すると…」を架空見出し〔WebAPサーバのメンテナンス〕の下に孤立させている。**見出し〔WebAPサーバのメンテナンス〕は架空**（本文は〔Webサイトの増設〕の続き）。削除して統合する（設問3が〔Webサイトの増設〕について、で空欄dまで含む整合のため）。

7. **メンテナンス手順の崩壊（行112〜118）**
   - 正：「WebAPサーバのメンテナンス時は、作業を行うWebサイトは停止する必要があるので、次の手順で作業を行う。<u>④メンテナンス中は、一つのWebサイトでサービスを提供することになるので、Mシステムを利用する顧客への影響は避けられない</u>。
     - (i) 事前にDNSサーバbのリソースレコードの `[d]` を小さい値にする。
     - (ii) メンテナンス作業を開始する前に、**メンテナンスを行うWebサイトの、インターネットに公開するドメインのWebAPサーバのFQDNに対応するAレコードを、DNSサーバb上で無効化する。**
     - (iii) この後、一定時間経てばメンテナンス作業が可能になるが、**作業開始が早過ぎると顧客に迷惑を掛けるおそれがある。そこで、<u>⑤手順(ii)でAレコードを無効化したWebAPサーバの状態を確認し</u>、問題がなければ作業を開始する。**」
   - MDは(ii)を「削除する」と誤記（正は「無効化」）、(iii)と後続段落が重複崩壊。

## B. 中程度（本文の誤変換）

1. **冒頭（行10）**：正「Mシステムは、Webアプリケーションサーバ（以下、**WebAPサーバ**という）、DNSサーバ、ファイアウォール（以下、FWという）などから構成されるWebサイトとF社の運用PCから構成される。現在、Webサイトは、**B社**のデータセンター（以下、b-DCという）に構築されている。」（MD「WebアプリケーションサーバAP（以下、APサーバという）」は誤り。全文で「WebAPサーバ」に統一）
2. **行49**：正「F社のL3SWの経路表には、**b-DCのWebサイトbへの経路**と<u>①デフォルトルート</u>が登録されている。」
3. **〔Mシステムの応答速度の低下〕冒頭（行57）**：正「最近、顧客から、Mシステムの応答が遅くなることがあるという苦情が、Mシステムのサポート窓口に入ることが多くなった。そこで、F社の情報システム部（以下、システム部という）の**運用担当者のD主任**は、運用PCを使用して次の手順で**原因究明**を行った。」
4. **(ii)（行60）**：正「FWbのログを**検査し、異常な通信は記録されていない**ことを確認した。」
5. **(iii)（行61）**：正「SSHを使用し、<u>③広域イーサ網経由でWebAPサーバbにログインしてCPU使用率を調べた</u>ところ、設計値を超えた値が**継続する時間帯**のあることを確認した。」
6. **〔Webサイトの増設〕冒頭（行69）**：正「D主任の判断を基に、システム部では、これまでのシステムの構築と運用の経験を生かすことができる、**現在と同一構成のWebサイトの増設**を決めた。」（c-DCの決定はE課長の指示文に含まれる）
7. **図2の説明（行79〜83）**：b-DCのDMZは**192.168.0.0/24**、c-DCのDMZは192.168.1.0/24（MDは両方1.0/24）。
8. **FWbのNAT（行47）**：正「FWbには、DMZに**設定された**プライベートアドレスと…表1に**示した**内容で…」（概ね一致、微修正）。
9. **設問1(2)の解説（行156〜160）**：書き直し。運用PC→**L3SW**→広域イーサ網→**FWb**→**L2SWb**→DNSサーバb（優先DNSサーバ=nsb.f-sha.example.lan=192.168.0.1はプライベートアドレスなので、インターネット（FWf）ではなく広域イーサ網経由。FWfは経由しない）。MDの「FWf（←経由なし？）」等の混乱文は削除。
10. **設問2(2)の解説（行170〜172）**：「広域イーサ網経由＝社内向けドメインのFQDN=apb.f-sha.example.lan（192.168.0.2）」に合わせて表1項番4の修正後の値で説明。

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1) | FWf | FWf | ○ |
| 設問1(2) | L3SW、FWb、L2SWb | 同一 | ○（解説をB-9で書き直し） |
| 設問2(1) a | miap.example.jp | 同一 | ○（根拠のドメイン記述をA-4で修正） |
| 設問2(2) | apb.f-sha.example.lan | 同一 | ○（表1をA-1で修正） |
| 設問3(1) b | DNSサーバc | 同一 | ○ |
| 設問3(1) c | ゾーン | ゾーン | ○（段落をA-6で修正） |
| 設問3(1) d | TTL | TTL | ○ |
| 設問3(2) | Mシステムの応答速度が低下することがある。 | 同旨 | ○（下線番号を④に修正） |
| 設問3(3) | ログイン中の利用者がいないこと | 同一 | ○（下線番号を⑤に修正） |

## D. その他・画像確認

- 画像参照 `../images/2023春_問05_図01・図02・表01・表02` の実在と内容を修正時に確認（表1・表2は誤記込みの画像でないか要確認）。
- キーワード表は概ね妥当。
