# 修正メモ：2022春_問03_プログラミング.md

照合元：`pdfs/2022r04h_ap_pm_qs.pdf` p.14〜19（問3本体）、公式解答 `pdfs/2022r04h_ap_pm_ans.pdf`
※**図5で空欄エに解答（0）が埋め込まれて空欄消失、図6の空欄ラベルが1つずれて空欄ケが丸ごと消失（設問2は正しくはオ〜ケの5空欄）**。設問1エの解答表が「x+1」と誤り（正: 0）、設問2オが「div(x,9)+9」と誤記（正: div(x,9)*9）、設問3の解答が「逆遷する」と崩壊（正: 退避する）。

## A. 重大（設問・解答に影響）

1. **図6の空欄ラベルずれ＋空欄ケの消失（行119〜151）— 設問2の根拠**
   - 正しい図6（空欄は**オ〜ケの5つ**）：
     ```
     function row_ok(n, x)               //横1行の重複チェック
       row_top ← [ オ ]                 //行の左端のマスを示す添字を求める
       for (i を 0 から 8 まで 1 ずつ増やす)
         if ( [ カ ] )
           return false
         endif
       endfor
       return true
     endfunction

     function column_ok(n, x)            //縦1列の重複チェック
       column_top ← [ キ ]              //列の上端のマスを示す添字を求める
       for (i を 0 から 8 まで 1 ずつ増やす)
         if ( [ ク ] )
           return false
         endif
       endfor
       return true
     endfunction

     function frame_ok(n, x)             //3×3の枠内の重複チェック
       frame_top ← x − [ ケ ] − mod(x, 3)  //枠内の左上端のマスを示す添字を求める
       for (i を 0 から 2 まで 1 ずつ増やす)
         for (j を 0 から 2 まで 1 ずつ増やす)
           if (board[frame_top ＋ 9 * i ＋ j] が n と等しい)
             return false
           endif
         endfor
       endfor
       return true
     endfunction
     ```
   - MDは row_top の空欄を「ア」、column_top を「カ」等とラベルが崩れ、frame_top の空欄が「ク」になって**ケが存在しない**。
   - 設問2の設問文 正：「図6中の **`[オ]` 〜 `[ケ]`** に入れる適切な字句を答えよ。」

2. **設問2の解答表の誤り・欠落（行205〜223）**
   - 公式解答：
     - **オ＝div(x, 9)*9**（MD「div(x, 9) + 9」は誤り）
     - カ＝board[row_top＋i]がnと等しい
     - キ＝mod(x, 9)
     - ク＝board[column_top＋9*i]がnと等しい
     - **ケ＝mod(div(x, 9)*9, 27)**（MDでは丸ごと欠落）
   - 行214〜223の「IPA公式」ブロックの「9+i」誤記、frame_topの捏造説明（行220〜221）を削除し、上記5空欄で書き直し。ケの解説：frame_top＝x−（行番号×9 を27で割った剰余…ではなく）**x − mod(div(x,9)*9, 27) − mod(x,3)**＝枠左上端。div(x,9)*9は行左端添字であり、その27の剰余が枠上端からの行のずれ×9になる。

3. **図5の空欄エ消失（行107）＋設問1エの解答誤り（行199〜201）**
   - 正：「board[x] ← `[ エ ]`　//再帰から戻った場合のマスの初期化」（MDは「board[x] ← 0」と解答を埋め込み）。
   - 公式解答 **エ＝0**。MD解答表の「エ＝x + 1」は誤り（行201の「IPA公式…エ=θ（0）」の崩壊表記も修正）。
   - ウの表記も公式は「**check_ok(n, x)がtrueと等しい**」（MD「check ok」のアンダースコア欠落を修正）。

4. **設問3の解答の崩壊（行229〜234）**
   - 公式解答：処理A＝「**データ構造Zを退避する**」、処理B＝「**退避したデータ構造Zを取り出す**」。
   - MDの「逆遷する」「逆遷した」は崩壊。解説の趣旨（更新処理で壊れる前の状態を保存し、バックトラック時に復元する）は妥当なので用語だけ修正。

5. **冒頭の説明の崩壊（行10）**
   - 正：「…このパズルは、**図1に示すように幾つかのマスに数字が入れられている状態**から、数字の入っていない各マスに、1〜9のうちのどれか一つの数字を入れていく。**このとき、盤面の横1行、縦1列、及び**太線で囲まれた3×3の枠内の全てにおいて、1〜9の数字が一つずつ入ることが、このパズルのルールである。」
   - MDの「9×9のマスを使うように数字が埋め込まれている状態から」「このことを、整数の配列とする。図1及び…」は崩壊。

6. **表2（データ構造Zの初期化・更新処理）の崩壊（行165〜168）**
   - 正：
     - 初期化処理「**初期化時の盤面に対し、個々の空白のマスについて1〜9の数字を入れた場合の重複チェックを行う。重複チェックの結果によって、初期化時の盤面の状態で個々の空白のマスに入れることができない数字は、データ構造Zの該当する数字の要素に0を設定する。それ以外の要素には1を設定する。**」
     - 更新処理「空白のマスに数字を入れたとき、**そのマスが含まれる横1行、縦1列、3×3の枠内の全てのマスを対象に、データ構造Zの該当する数字の要素を0に更新する**。」
   - MDの「空白でないマスについて、そのマスの数字のみが1となるように設定する」は捏造。

7. **データ構造Zの定義の崩壊（行159）**
   - 正：「そこで、**重複チェックの実行回数を少なくするために、各マスに入れることができる数字を保持するための**データ構造Zを考える。データ構造Zは盤面のマスの数×9**の要素をもち、添字xは0から、添字nは1から始まる**2次元配列とする。**Z[x][n]は、ゲームのルールにのっとってboard[x]に数字nを入れることができる場合は要素に1を、できない場合は要素に0を格納する。**データ構造Zの初期化処理と更新処理を表2のように定義した。**なお、データ構造Zは大域変数として導入する。**」

8. **改善後の手順の崩壊（行170〜175）**
   - 正：「〔パズルを解く手順〕の**(1)の前にデータ構造Zの初期化処理を追加し**、〔パズルを解く手順〕の(2)〜(5)を次の(2)〜(4)のように変更した。」
   - (3) 正：「データ構造Zを参照し、(2)で見つけた空白のマスに入れることができる**数字のリストを取得し、リストの数字を順番に入れる**。」（MD「リストの先頭に入れる」は誤り）
   - (3-2) 正：「入れる数字がない場合、マスを空白に戻し、<u>②処理B</u>を行った後、一つ前に数字を入れたマスに戻り、**戻ったマスで取得したリストの次の数字から再開する**。」

## B. 中程度（本文の誤変換）

1. **盤面の表現（行41）**：正「各要素には**マスに入れられた数字**が格納され、空白の場合は0を格納する。」
2. **表1（行64〜78）**：
   - solve(x) 正「パズルを解くための手順を実行する関数。**盤面を表すboard[]の添字xを引数とする**。」
   - row_ok/column_ok/frame_ok 正「…チェックを行う関数。**チェック対象の数字n、チェック対象のマスを示す添字xを引数とする。数字の重複がない場合はtrue、重複がある場合はfalseを返す。**」
   - div/mod 正「**div(n, m)**：整数nを整数mで割った商を求める関数」「**mod(n, m)**：整数nを整数mで割った剰余を求める関数」（MDは div(a, m)）
   - row_top 正「…横1行の**左端**のマスを示す添字を格納する変数」（MD「上端」）
   - MAX_BOARD 正「盤面に含まれるマスの数を表す定数で**81**。」
3. **図5のコメント（行100〜107）**：「//対象のマスが空白でない場合」「//次の探索」「//1〜9の数字を順にマスに入れる」「//再帰から戻った場合の**マスの**初期化」に合わせる。
4. **手順(3)(4)（行28〜29）**：正「(3) **(2)で見つけた**空白のマスに、1〜9の数字を順番に入れる。」「(4) 数字を入れた**ときに**、その状態が…」

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 ア | board[x]が0でない | 同一 | ○ |
| 設問1 イ | x＋1 | 同一 | ○ |
| 設問1 ウ | check_ok(n, x)がtrueと等しい | 同旨（表記修正） | ○ |
| 設問1 エ | 0 | 表は「x + 1」 | **×要修正（A-3）** |
| 設問2 オ | div(x, 9)*9 | 「div(x, 9) + 9」 | **×要修正（A-2）** |
| 設問2 カ | board[row_top＋i]がnと等しい | 同一 | ○ |
| 設問2 キ | mod(x, 9) | 同一 | ○ |
| 設問2 ク | board[column_top＋9*i]がnと等しい | 同旨（「9+i」誤記修正） | ○ |
| 設問2 ケ | mod(div(x, 9)*9, 27) | **丸ごと欠落** | **×要修正（A-1/A-2）** |
| 設問3 処理A | データ構造Zを退避する | 「データ構造Zを逆遷する」 | **×要修正（A-4）** |
| 設問3 処理B | 退避したデータ構造Zを取り出す | 「逆遷したデータ構造Zを取り出す」 | **×要修正（A-4）** |

## D. その他・画像確認

- 画像参照 `../images/2022春_問03_図01・図03・表01・図05・図06・表02` の実在と内容を修正時に確認（図6は空欄オ〜ケの5つであること）。
- キーワード表は概ね妥当。
