# 修正メモ：2024春_問07_組込みシステム.md

照合元：`pdfs/2024r06h_ap_pm_qs.pdf` p.40〜43（問7本体）、公式解答 `pdfs/2024r06h_ap_pm_ans.pdf`
※解答は全問公式と一致。ただし本文・表・図の再現に重大な崩れあり。

## A. 重大（解答の根拠・設問に影響）

1. **カップ判定のタイミングが真逆（行39）**
   - 現状：「カップ判定は、利用者がドアを**開いた時**に…撮影して行う」
   - 正：「カップ判定は、利用者が**ドアを閉じた時**に、カメラでカップ載置部を複数回撮影して行う。」（状態遷移・設問全体の前提）

2. **動作概要(6)の「ドアをロック解除し」が欠落（行68）— 設問3(1)の根拠**
   - 正：「(6) コーヒーの排出が終わると、**ドアをロック解除し**、タッチパネルにカップの引取りを促す画面を表示する。」
   - 設問3(1)の答え（"確認"→ロック、"抽出完了"→ロック解除）は、(5)の空欄a=ドアをロック と (6)のロック解除が対になっている。
   - (5)も正しくは「利用者が確認ボタンにタッチすると、`[a]` し、カップのサイズに応じた**分量**のコーヒーを抽出して**コーヒー排出口からカップに注ぎ込む**。」

3. **表3（タスクの処理概要）が崩壊（行85〜91）— "中止"／"中止完了"、時間仕様が全欠落**
   - 正しい内容：
     - メイン：「コーヒーマシンの状態管理を行う。」（1行のみ。MDは他タスクの説明が混入）
     - カップ判定：「メインタスクから"判定"を受けると、カメラで複数回撮影を行い、得られた画像データを用いてカップ判定を行った後、結果を**"判定結果"**でメインタスクに通知する。**判定に掛かる時間は、300ミリ秒以上500ミリ秒以下である。**」「**カップ判定中にメインタスクから"中止"を受けると、5ミリ秒以内にカップ判定を中止して"中止完了"をメインタスクに通知する。カップ判定中以外で"中止"を受けたときは無視する。**」（MDの「サブタスクからの"抽出中止"」は架空。"中止完了"は図4・設問3の重要メッセージ）
     - 抽出：「メインタスクから"抽出"を受けると、**抽出部を起動して抽出を開始し、抽出部から抽出終了を受信するまで待つ**。」「**抽出終了を受信すると、コーヒーの排出が終了したと判断し、抽出部を停止して"抽出完了"をメインタスクに通知する。**」
     - タッチパネル：「メインタスクから"画面表示"を受けると、指定された画面をタッチパネルに表示する。」「**利用者が確認ボタンに触れたことを検出すると、"確認"をメインタスクに通知する。**」
     - ドア：「開閉センサーの出力を10ミリ秒周期で読み出し、確定したドアの開閉状態を保持する。」「**確定したドアの開閉状態が変化したら、"ドア開"又は"ドア閉"をメインタスクに通知する。**」（設問2(3)の前提）「メインタスクから"ロック"又は"ロック解除"を受けると、ロック機構を操作して、ドアをロック又はロック解除する。」

4. **図4の状態遷移の説明が誤り（行109〜114）— 「ドア開」状態の消失**
   - 正しい遷移（図4）：
     - 待機中 --**"ドア閉"**--> カップ判定（MDの「待機中→(ドア開)→カップ判定」は誤り）
     - カップ判定 --"判定結果"(カップなし)--> 待機中
     - カップ判定 --"判定結果"(空カップあり)--> 確認待ち
     - カップ判定 --"判定結果"(カップあり又は障害物あり)--> 引取り待ち
     - カップ判定 --"ドア開"--> 中止待ち
     - **中止待ち --"中止完了"又は"`[b]`"--> ドア開（状態）**
     - **ドア開（状態） --"ドア閉"--> カップ判定**
     - 確認待ち --"ドア開"--> ドア開（状態）
     - 確認待ち --"確認"--> 抽出中
     - 抽出中 --"抽出完了"--> 引取り待ち
     - 引取り待ち --"ドア閉"--> カップ判定
   - MDには**状態「ドア開」が存在しない**。空欄bの遷移は「中止待ち→ドア開」であり、MDの「各状態から待機中へ」は誤り。
   - 設問3(2)の解説（行181〜183）も書き直し：中止待ちは「カップ判定中にドアが開けられ、"中止"を送った後の状態」。カップ判定タスクが"中止"を受ける前に判定を終えていた場合は"中止完了"ではなく**"判定結果"**が届くため、b=判定結果。（MDの「カップなしの結果を受けて待機中に戻る」は誤り）

5. **表1（構成要素）の崩壊（行26〜33）— カップ判定の定義箇所**
   - 制御部 正：「コーヒーマシン全体の制御及び状態管理を行う。また、カップの有無やサイズ、**カップが空か否かを判定（以下、カップ判定という）するための画像認識**を行う。」
   - タッチパネル：「制御部から指示された画面を表示する。」「**利用者がタッチした座標情報を制御部に通知する。**」（欠落）
   - 抽出部：「制御部から指示された**分量**のコーヒーを抽出し、コーヒー排出口から排出する。」（MD「サイズ」）
   - ドア 正：「開閉センサーをもち、**開閉状態を0/1のデジタル信号で制御部に入力する**。」「**ドアを閉じた状態でロックすることができる**ロック機構をもつ。ロック機構は、制御部からの指示でロック及びロック解除ができる。**ロック及びロック解除に掛かる時間は無視できるほど小さいものとする。**」（MDの「閉状態は1と開状態は0」は誤り。閉=0/開=1は判定仕様の節に記載）
   - **カップ載置部とカメラの説明が入れ替わって崩壊**。正：カップ載置部=「コーヒー排出口から排出されたコーヒーを受けるカップを置く場所である。」カメラ=「カップ載置部を撮影するカメラで、制御部からの指示で撮影を行い、**制御部と共有するメモリに画像データを書き出す**。」

6. **開閉センサー仕様の誤記（行56）**
   - 「ドアが完全に閉じているときは**0**、それ以外は1を出力する」（MD「8」は誤植）。「**非常に**短い間隔で0と1とを交互に出力することがあるので」

7. **図3の説明の矛盾と注記欠落（行99〜101）**
   - MD行100「アの時点では…**閉状態**」→ 設問2(3)の前提は「**開状態**」。矛盾記述を削除。
   - 注記2「**クとケの間は、開閉センサーの出力は常に0である。**」が欠落（ドア閉=カ の確定に必要な情報）。
   - 設問2(3)の記号範囲：「ア〜**テ**」（MD「アーケ」は誤り）。

## B. 中程度（本文の誤変換）

1. 表2（行48〜50）：「専用の**紙**カップが、空の状態で…」「専用の紙カップが**空でない状態で**カップ載置部に置かれている」（MD「定まった状態」は崩壊）
2. 動作概要(1)（行63）：「初期化処理が完了したら**待機中**となり、**カップをカップ載置部に置くように促す画面をタッチパネルに表示する**。」（MD「特機中」「何もないように促す」崩壊）
3. 動作概要(3)(4)(7)(8)(9)：「(3) 利用者がドアを閉じると、カップ判定を行う。」「(4) …結果が"空カップあり"となる**ので**、カップのサイズを表す文字と、確認ボタン**で構成される画面**を表示する。」「(7) 利用者が**ドアを開け**、カップを引き取る。」「(8) 利用者がドアを閉じると、カップ判定を行う。」「(9) …となる**ので**、待機中に戻る。」
4. 設問1(2)（行124）：「カウント値が0になったら、**次のカウントダウンまでの間に初期値をリロードして動作を継続する**。タイマーに設定する初期値は**幾つか**、整数で求めよ。ここで、1k=10³とする。」
5. 設問2(2)（行130）：「"抽出"を通知する際の**パラメータとして、必要な情報**を答えよ。」（MD「必要な情報パラメータとして」重複）
6. 図3見出し（行95）：「**開閉センサー**の出力と、ドアタスクの動作タイミングの例」（MD「割込みセンサー」誤り）

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1) a | ドアをロック | ドアをロック | ○ |
| 設問1(2) | 320 | 320 | ○ |
| 設問2(1) | カップ判定中にドアが開けられたことを検出したいから | 同一 | ○ |
| 設問2(2) | コーヒーの分量 | コーヒーの分量 | ○ |
| 設問2(3) | ドア開=タ、ドア閉=カ | 同一 | ○（前提の矛盾をA-7で修正） |
| 設問3(1) | "確認"、"抽出完了" | 同一 | ○（根拠のロック解除欠落をA-2で修正） |
| 設問3(2) b | 判定結果 | 判定結果 | ○（解説はA-4のとおり書き直し） |

## D. その他・画像確認

- 画像参照 `../images/2024春_問07_図01_外観と内部構成.png`、`表02_カップ判定結果.png`、`表03_タスク処理概要.png`、`図03_タイミング図.png`、`図04_状態遷移図.png` の実在と内容を修正時に確認。特に図4は「ドア開」状態を含む正しい遷移図か要確認。
- キーワード表（行189〜）は妥当。
