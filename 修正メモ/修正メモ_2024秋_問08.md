# 修正メモ：2024秋_問08_情報システム開発.md

照合元：`pdfs/2024r06a_ap_pm_qs.pdf` p.42〜45（問8本体）、公式解答 `pdfs/2024r06a_ap_pm_ans.pdf`
※解答はほぼ一致するが、図2の説明が崩壊（空欄dが消失）、設問2の解答群が架空、設問構成に誤りあり。

## A. 重大（解答・設問に影響）

1. **図2（クラス図）の説明が崩壊 — 空欄dが図から消失（行47〜74）**
   - 正しいクラス構成：
     - **連携データ**（斜体＝抽象）：#論理削除 : boolean、※省略 ─ 継承 → **組織データ**（※省略）
     - **《interface》コネクタ**：+data : 連携データ／+form : フォーマット／+pro : プロトコル、+データ取得()／+データ送信(data : 連携データ)
     - **組織コネクタ**（コネクタを継承）：※省略、**+`[d]`**／+データ送信(data : 組織データ)／《Create》+組織コネクタ(form : フォーマット, pro : プロトコル) : 組織コネクタ
     - **フォーマット**：※省略、+シリアライズ(data : 連携データ)／**+デシリアライズ() : 連携データ** ─ 継承 → **CSV**、**XML**（各同シグネチャ）
     - **プロトコル**（斜体＝抽象）：※省略、+取得(form : フォーマット) : 連携データ／+送信(data : 連携データ, form : フォーマット) ─ 継承 → **FTPS**、**HTTPS**
     - 凡例：−は私的、+は公開、#は保護。**斜字体は抽象を意味する。**継承（白抜き三角）、関連（実線）。
   - **空欄dは組織コネクタクラスの1番目のメソッド**（公式解答：**データ取得()**）。MDの図2説明にはdが存在せず、「論理属性: boolean」「組織コネクタ(cla: 組織コネクタ)」等の架空記述がある。

2. **設問2の構成と解答群が架空（行102〜114）**
   - 正しい構成：
     - 設問2(1)：「**図2中の** `[d]` に入れる適切な字句を答えよ。」→ データ取得()
     - 設問2(2)：「本文中の `[e]`、`[f]` に入れる適切な字句を**解答群の中から**選び、記号で答えよ。」解答群：「ア オーバーライド／イ オーバーロード／ウ コンストラクタ／エ できない／オ できる／カ デストラクタ」→ **e=オ（できる）、f=ア（オーバーライド）**
     - 設問2(3)：「本文中の `[g]`、`[h]` に入れる適切なクラス名を**図2中の字句を用いて**答えよ。」→ g=XML、h=HTTPS
   - MDの解答群「ア できる／イ できない」は架空。MD解答の「e=ア（できる）」は記号が誤り（正=オ）。

3. **設問3の設問文が不正確（行118）**
   - (1) 正：「本文中の下線①について、**作成する二つのクラスは、図2中の異なるクラスを親クラスとする必要がある。作成するクラスの親クラスとすべきクラス名**を図2中の字句を用いて**全て**答えよ。」→ 公式解答：**連携データ、コネクタ**
   - (2) 正：「本文中の下線②について、文字コード変換のメソッドを**追加すべきクラス名を図2中の字句を用いて**答えよ。」→ フォーマット

4. **冒頭段落の欠落（行12）**
   - 正：「M社には**三つの事業所に**合計約100名が在籍しており…ERPソリューションやクラウドサービスの仕様変更に合わせて**頻繁に**修正する必要がある。また、同じプログラムコードが複数箇所にコピーされているなどの原因によって保守性が低いという課題があり、**コネクタの開発コストが増大している。そこで、M社では、コネクタの開発コストを低減させるために、コネクタを構成するソフトウェアを再設計することになり**、開発部のN君が担当することになった。」

## B. 中程度（本文の誤変換）

1. 行35：「この**方法**では…`[a]` という**設計指針**のもと、外部公開するメソッドを定義して**オブジェクト内のデータや処理の変更がオブジェクト外に与える影響を小さくする**。」「**開発コスト**を低減できる」（MD「関発コスト」）「同名のメソッド呼出しに対して**オブジェクトの種類**によって…」
2. 行78 正：「フォーマットクラス、CSVクラス、XMLクラスは、**送信するデータ**をCSV形式やXML形式に変換（シリアライズ）したり、**受信したCSV形式やXML形式のデータを解析して**、連携データクラスのインスタンスを生成（デシリアライズ）したりするクラスである。また、XMLクラスのシリアライズのように、親クラスと同じ**名称**・引数・戻り値のメソッドを**定義する**ことを `[f]` という。」
3. 行80 正：「プロトコルクラス、FTPSクラス、HTTPSクラスは、データ送受信のためのクラスである。**コネクタクラス、組織コネクタクラスは、コネクタを実現するためのクラスであり、送受信するデータのデータ項目に応じて処理を行うものである。**」
4. 行76：「…連携データクラスと**それを親クラスとする**組織データクラスを設計し、**各クラスに必要な属性とメソッドを設計した**。」
5. 行88：「…図2で設計したクラスに対して少ない修正で対応できるか**机上で検証した**。」（MD「柔軟性を検証」）
6. 行90：「②図2の**ある一つの**クラスに文字コード変換のメソッドを追加すればよい。」（下線範囲を正しく）
7. 設問1の解答表記：公式は c=**多相性**（MD「多態性（ポリモーフィズム）」→公式表記を主に、括弧で併記可）

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 a | カプセル化 | カプセル化 | ○ |
| 設問1 b | 継承 | 継承 | ○ |
| 設問1 c | 多相性 | 多態性（ポリモーフィズム） | △（公式表記に統一） |
| 設問2(1) d | データ取得() | 記載あり（ただし図2にd欠落） | △（図2を修正） |
| 設問2(2) e | オ（できる) | ア（できる）と誤記 | **×（解答群ごと修正）** |
| 設問2(2) f | ア（オーバーライド） | ア | ○（解答群を復元） |
| 設問2(3) g | XML | XML | ○ |
| 設問2(3) h | HTTPS | HTTPS | ○ |
| 設問3(1) | 連携データ、コネクタ | 同旨（設問文が不正確） | △（設問文をA-3のとおり修正） |
| 設問3(2) | フォーマット | フォーマット | ○ |

## D. その他・画像確認

- 画像参照 `../images/2024秋_問08_図01_データ連携ハブ.png`、`図02_クラス図.png` の実在と内容を修正時に確認（図2は空欄d入りの正しい図か要確認）。
- キーワード表は概ね妥当（「多態性」→「多相性（多態性）」に揃える）。
