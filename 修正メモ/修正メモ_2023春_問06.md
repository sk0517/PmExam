# 修正メモ：2023春_問06_データベース.md

照合元：`pdfs/2023r05h_ap_pm_qs.pdf` p.32〜36（問6本体）、公式解答 `pdfs/2023r05h_ap_pm_ans.pdf`
※解答の値は全問公式と一致。ただし**空欄c・d・下線①が出現する「H主任の考慮」段落が本文から丸ごと欠落**、図2のSQLが多数崩壊（SUM(COALESCE(…))消失、FROM句の役職B欠落等）、表1〜表4・E-R図説明も崩壊が激しい。

## A. 重大（設問・解答の根拠に影響）

1. **「H主任の考慮」段落の丸ごと欠落 — 空欄c・d（本文側）と下線①の出現箇所**
   - p.35末尾に以下を復元（〔達成状況集計リストの作成〕内、図2の説明の後）：
     「H主任は、図2の項番4のSQL文の設計の際に、次に示す考慮を行った。
     ・表2の評価方法に従い、管理職の従業員データは対象に含めず、年度途中入社と、年度途中退職の従業員データについては出力しないように、抽出日に退職している従業員データを出力しない"従業員_所属_一時表"と、年度開始時点で入社していない従業員データを出力しない"従業員ごと_目標集計_一時表"を `[c]` によって結合しておく。
     ・`[c]` による結合結果と、実績がある場合だけレコードの存在する"従業員ごと_実績集計_一時表"を `[d]` によって結合しておく。また、<u>①実績個人集計がNULLの際は、0を設定しておく</u>。」
   - 設問2(2)の設問文も修正 正：「本文中の下線①に示す事態は、**年度開始年月日から集計年月日までの間に、どのデータがどのような場合に発生するか。**40字以内で答えよ。」（MDの「年度途中に入社した従業員で…」は捏造）。解説も「日別個人実績は日別実績のない従業員のレコードを作成しない→年度開始から集計日まで実績が1件もない従業員はLEFT OUTER JOINでNULLになる」に書き直し。

2. **図2（SQL）の崩壊（行120〜153）**
   - 正しいSQL（p.36）：
     - 項番1：`INSERT INTO 従業員_所属_一時(従業員コード, 組織コード)`／`SELECT A.従業員コード, A.所属組織コード FROM 所属 A, 役職 B`／`WHERE TO_DATE(:集計年月日) ［e］ A.所属開始年月日 AND A.所属終了年月日`／`AND A.役職コード = B.役職コード AND ［f］`
       （MDは表名「従業員ごと_所属_一時」・列リストに「役職B」混入・**FROM句の`役職 B`が欠落**）
     - 項番2：`SELECT 従業員コード, KPIコード, SUM(月別目標値) FROM 月別個人目標 WHERE 年月 ［e］ :年度開始年月 AND :集計年月`／`［g］`（MDの`SUM(日別実績値)`は誤り）
     - 項番3：`SELECT 従業員コード, KPIコード, SUM(日別実績値) FROM 日別個人実績 WHERE 年月日 ［e］ TO_DATE(:年度開始年月日) AND TO_DATE(:集計年月日)`／`［g］`（MDは項番2と同じWHEREに崩壊）
     - 項番4：`INSERT INTO ［h］(組織コード, KPIコード, 目標組織集計, 実績組織集計, 対象従業員数)`／`SELECT A.組織コード, B.KPIコード, SUM(B.目標個人集計), SUM(COALESCE(C.実績個人集計, 0)), ［i］`／`FROM 従業員_所属_一時 A`／`［c］ 従業員ごと_目標集計_一時 B ON A.従業員コード = B.従業員コード`／`［d］ 従業員ごと_実績集計_一時 C ON B.従業員コード = C.従業員コード AND B.KPIコード = C.KPIコード`／`GROUP BY A.組織コード, B.KPIコード`
       （MDは**`SUM(COALESCE(C.実績個人集計, 0))`が`SUM(C.実績個人集計)`に崩壊**。下線①＝COALESCEの根拠）
     - 項番5：`SELECT A.*, A.目標組織集計/A.対象従業員数, A.実績組織集計/A.対象従業員数 FROM ［h］ A ORDER BY A.組織コード, A.KPIコード`（MDの「A.a, A.目標組織集計数…」は崩壊。目標平均値・実績平均値の算出）
   - 図2直後の説明段落も復元：「ここで、TO_DATE関数は、指定された年月日をDATE型に変換するユーザー定義関数である。関数COALESCE(A,B)は、AがNULLでないときはAを、AがNULLのときはBを返す。また、":年度開始年月日"、":年度開始年月"、":集計年月日"、":集計年月"は、該当の値を格納する埋込み変数である。」（MDでは欠落）

3. **表4（集計リスト作成処理の概要）の崩壊（行106〜112）**
   - 正：
     | 項番 | 入力表 | 出力表 | 集計日における処理内容 |
     |---|---|---|---|
     | 1 | **所属、役職** | **従業員_所属_一時** | **一般職従業員と所属組織の対応表を作成する。** |
     | 2 | 月別個人目標 | 従業員ごと_目標集計_一時 | 年度開始年月から集計月までの従業員、KPI項目ごとの**目標個人集計値**を求める。 |
     | 3 | 日別個人実績 | 従業員ごと_実績集計_一時 | 年度開始年月日から集計日までの従業員、KPI項目ごとの**実績個人集計値**を求める。 |
     | 4 | 項番1〜3の出力表 | 組織ごと_目標実績集計_一時 | 組織、KPI項目ごとの目標集計値、実績集計値、従業員数を求める。 |
     | 5 | 項番4の出力表 | **−** | 組織、KPI項目ごとの目標集計値、実績集計値、従業員数、**目標平均値、実績平均値**を一覧化した集計リストを作成する。 |
   - MDの項番1「組織、従業員、所属」入力・「集計年月の年月日1日から…」等は崩壊。一時表名は全文で「**従業員_所属_一時**」（「従業員ごと_所属一時」ではない）。

4. **図1（E-R図）の説明崩壊（行56〜70）— 設問1の対象**
   - 正しいエンティティ構成：
     - **組織**：組織コード(主キー)、**組織開始年月日(主キー)**、組織終了年月日、組織名、上位組織コード(外部キー)、上位組織開始年月日(外部キー)。組織から組織への自己参照の関連（1対多）。
     - **所属**：従業員コード(主キー)、所属開始年月日(主キー)、所属終了年月日、所属組織コード(外部キー)、所属組織開始年月日(外部キー)、役職コード(外部キー)
     - **役職**：役職コード(主キー)、役職名、職務区分。役職→所属（1対多）。
     - **従業員**：**`[b]`(主キー)**、氏名、生年月日、メールアドレス、住所。従業員→所属（1対多）、従業員→月別個人目標（1対多）、従業員→日別個人実績（1対多）。
     - **月別個人目標**：従業員コード(主キー)、KPIコード(主キー)、**年月(主キー)**、月別目標値
     - **日別個人実績**：従業員コード(主キー)、KPIコード(主キー)、**年月日(主キー)**、日別実績値
     - **KPI**：KPIコード(主キー)、年度、KPI項目名、定量的成果目標。KPI→月別個人目標、KPI→日別個人実績（各1対多）。
     - **`[a]`は組織と所属の間の関連**（空欄。公式解答は「→」＝組織から所属への1対多）。
   - 注記 正：「属性名の実線の下線は主キー、破線の下線は外部キーを示す。**主キーの実線が付いている属性名には、外部キーの破線を付けない。**」（MDの「スーパーキー」記述は崩壊）
   - MDの「組織」に月別個人目標FKが混入、「従業員」に所属属性が混入、月別/日別の主キーが「年度・月・日」になっている等すべて修正。
   - **設問1の解説（行175〜179）も書き直し**：a＝組織と所属の間の**1対多の関連（→）**（1つの組織に複数の従業員が所属）／b＝従業員エンティティの主キー**従業員コード**（所属・月別個人目標・日別個人実績から参照される）。MDの「月別個人目標と役職の関連」は誤り。

5. **表1（G社の組織の例）の崩壊（行18〜22）**
   - 正：
     | 1階層 | 2階層 | 3階層 | 組織の説明 |
     |---|---|---|---|
     | **監査室** | − | − | **単独階層の組織** |
     | 総務部 | 人事課 | − | 全社共通のスタッフ組織 |
     | 技術開発部 | オープンソース推進課 | − | **全社共通の開発組織** |
     | **金融システム本部** | **証券システム部** | **証券開発課** | **業種業界ごとの開発組織** |
   - MDは監査室行が欠落し、「命令システム本部」「証券券部」「証券券発券グループ」等の誤変換。

6. **表2（追加したKPIの項目）の評価方法崩壊（行36〜40）**
   - 正：評価方法は3項目**共通**のセルで、
     「・一般職従業員の個人実績を**組織単位で集計し、平均値の達成状況を評価する**。
     ・年度途中入社、年度途中退職した従業員は、**評価対象外**とする。
     ・個人実績の集計は、**集計日時点で従業員の所属している直属の組織**に対して行う。**所属組織の上位階層、又は下位階層の組織の集計には含めない。**」
   - MDは各KPI行に表3の内容が混入した崩壊文。定量的成果目標（1,980時間以内／16日以上／6日以上）は一致。

7. **表3（管理内容と運用方法）の崩壊（行82〜86）**
   - 正：
     - KPI：「KPI項目と定量的成果目標を管理する。」「・参照だけ（更新は行わない）。」
     - 月別個人目標：「個人ごとの月別目標値を管理する。」「・年度開始時点で在籍している全従業員に対して、**当該年度分のレコードを、目標値を0として初期作成する**。・初期作成したレコードに対して、**各人で定量的成果目標を意識した月別目標値を入力し**、定期的に見直し、更新する。・**年度途中入社の従業員については、初期作成レコードが存在しない。月別目標値の入力も行わない。**・**管理職従業員はKPI評価対象外であるが、月別目標値の入力は一般職従業員と同様に行う。**」
     - 日別個人実績：「個人ごとの日別実績値を管理する。」「・**勤怠管理システム、研修管理システムで管理している追加したKPI項目に関する全従業員の実績値を基に、日次バッチ処理によってレコードを作成する。**・**日別実績のない従業員**のレコードは作成しない。」（下線①・設問2(2)の根拠）
   - MDの「目標値8」「退職した従業員に関するレコードは作成しない」等は崩壊。

8. **終了年月日の設定の誤り（行88）**
   - 正：「組織表の"組織終了年月日"と所属表の"所属終了年月日"には、過去の実績値、又は予定を設定する。**終了予定のない場合は9999年12月31日を設定する。**」
   - MDの「組織終了年月日が将来に及ぶ場合、値はNULLを設定する」は誤り（NULLではなく9999-12-31。項番1のBETWEEN判定が成立する根拠）。行24の同様の記述も削除。

## B. 中程度（本文の誤変換）

1. **冒頭（行10〜24）**：正「…人材の流動性を高めてきた。」「G社の組織は、表1の例に示すように最大三つの階層から構成されている。」「**従業員の職務区分には管理職、一般職の二つがあり、1階層から3階層のそれぞれの組織には1名以上の従業員が所属している。なお、複数階層、複数組織の兼務は行わない規定であり、従業員は一つの組織だけに所属する。**」（MD行24の崩壊文と差し替え）
2. **〔KPIの追加〕（行30）**：正「…従来のKPIに加え、**働き方改革、従業員満足度向上に関する**KPIの項目を今年度から追加することにした。」
3. **Kシステムの提供内容の欠落（行42の後）**：復元「Kシステムでは、次に示す仕組みと情報を提供する。・従業員各人が、月ごとの目標を設定する仕組み ・日々の実績を月次で集計し、各組織がKPI達成状況を評価するための情報」
4. **〔データベースの設計〕（行48〜50）**：正「H主任は、このシステムのためのE-R図を基に、…三つのエンティティを追加して、**KシステムのためのE-R図を作成する**ことにした。」「…適切なデータ型で**表定義した**関係データベースによってデータを管理する。」
5. **〔達成状況集計リストの作成〕（行96）**：正「H主任は、各組織がKPI達成状況を評価するための**情報として**、毎月末に達成状況集計リスト（以下、集計リストという）を**作成し**、提示することにした。」
6. **見出し**：MDの「〔表の管理内容と運用方法〕」見出しは架空（本文は〔データベースの設計〕の続き）。削除して統合。
7. **設問1（行159）**：正「図1中の `[a]`、`[b]` に入れる適切なエンティティ間の関連及び属性名を答え、**E-R図を完成させよ**。なお、エンティティ間の関連及び属性名の表記は、図1の凡例及び注記に**倣う**こと。」

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 a | →（組織→所属の1対多） | 「→（多対多）」等の混乱 | △（値は→だが解説誤り。A-4で書き直し） |
| 設問1 b | 従業員コード | 従業員コード | ○ |
| 設問2(1) c | INNER JOIN | INNER JOIN | ○（本文段落をA-1で復元） |
| 設問2(1) d | LEFT OUTER JOIN | LEFT OUTER JOIN | ○（同上） |
| 設問2(1) e | BETWEEN | BETWEEN | ○ |
| 設問2(1) f | B.職務区分 = '02' | 同一 | ○ |
| 設問2(1) g | GROUP BY 従業員コード, KPIコード | 同一 | ○ |
| 設問2(1) h | 組織ごと_目標実績集計_一時 | 同一 | ○ |
| 設問2(1) i | COUNT(*) | COUNT(*) | ○ |
| 設問2(2) | 該当従業員のKPI項目に対する実績データが、1件も存在しない場合 | 同一 | ○（設問文をA-1で修正） |

## D. その他・画像確認

- 画像参照 `../images/2023春_問06_図01・図02・表01〜表04` の実在と内容を修正時に確認（図2は崩壊SQLの画像でないか要確認）。
- キーワード表は概ね妥当（COALESCEの説明は「AがNULLでないときはA、NULLのときはB」に正確化）。
