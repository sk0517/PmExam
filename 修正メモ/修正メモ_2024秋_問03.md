# 修正メモ：2024秋_問03_プログラミング.md

照合元：`pdfs/2024r06a_ap_pm_qs.pdf` p.14〜17（問3本体）、公式解答 `pdfs/2024r06a_ap_pm_ans.pdf`
※図1のプログラムが完全崩壊、設問2イの解答が誤り、設問4はN=20（MDは28）と根本的な誤りあり。

## A. 重大（解答・設問に影響）

1. **図1（prime1）のプログラムが完全崩壊（行18〜45）**
   - MDのコード（while(dが2以上)/d mod 2/i←i+1 など）は架空。正しいプログラム：
     ```
     ○整数型の配列: prime1(整数型: N)
       整数型の配列: primes ← {}   /* 結果となる素数の一覧を格納する一次元配列 */
       論理型: isPrime             /* ループ内で着目している数が素数か否かを表す変数。
                                      trueであれば素数であることを表し、falseであれば
                                      素数ではないことを表す */
       整数型: d ← 2
       整数型: t
       /* メイン処理開始 */
       while (d が N 以下)
         isPrime ← true            /* 仮で素数として扱う */
         t ← 2
         while (t が d 未満)
           if (d mod t が 0 と等しい)
             isPrime ← false
           endif
           t ← t + 1               ←(L1)
         endwhile
         if (isPrime が true と等しい)
           primes の末尾に d の値を追加する
         endif
         d ← d + 1
       endwhile
       /* メイン処理終了 */
       return primes
     ```
   - 注意：prime1のisPrimeは**配列ではなく論理型のスカラー変数**。(L1)は「t ← t + 1」の行。

2. **設問2 イの解答が誤り（行180〜182）**
   - 公式：**イ=isPrime[d]がtrueと等しい**（MDの「isPrime[d-1]」は誤り。isPrimeへの追加はc=2から始まるが要素番号は1始まりでisPrime[k]がkに対応…ではなく、図2の注釈どおり「isPrime[k]がtrueであればkが素数」。解説も書き直し）
   - 図2のコメントも正:「/* isPrime[k]がtrueであれば**kが素数である**ことを表し、falseであれば**kが素数ではない**ことを表す一次元配列 */」（MDは2k+1と書いており、これは図3のコメント）

3. **設問4がN=20（MDはN=28）— 数値・解答とも誤り**
   - 正：「**prime1(20)、prime2(20)、prime3(20)**をそれぞれ実行したとき、図1中の(L1)の行、図2中の(L2)の行、図3中の(L3)の行が実行される回数をそれぞれ答えよ。」
   - 公式解答：**(L1)=171、(L2)=13、(L3)=2**
   - 解説の書き直し：
     - L1: 各dで内側ループは t=2〜d-1 の d-2 回 → Σ_{d=2}^{20}(d-2)=0+1+…+18=**171回**
     - L2: d=2: t=4,6,…,20 で9回、d=3: t=9,12,15,18 で4回、d=5以降: t=25>20 で0回 → **13回**
     - L3: d=3: t=9,15（t←t+2×3=+6、21>20で終了）→ **2回**
   - MDの表（28、13の半分以下等）と計算過程は全て削除して書き直す。

4. **〔アルゴリズムの改良1〕の手順(b)②が逆（行60）**
   - 正：「② **d以上**の自然数xについて、**dをx倍した数**を"素数ではない"とマークする。」（MD「d以下」は誤り）
   - 冒頭文 正：「素数の定義によって、2以上の自然数sについて、**s自身を除くsの正の倍数uは、1とu以外にsも約数に含むので素数ではない。この性質を利用して関数prime1を改良し、次の手順で素数を列挙する関数prime2を考える。**」（MD行53は「s以下の正の倍数」「関数prime2はこの事実を用いて、関数prime2を…改良した関数prime2」と崩壊）

5. **〔アルゴリズムの改良2〕の変更点（行105〜107）**
   - (1) 正：「**関数の戻り値として素数の一覧が格納される**primesにあらかじめ2を格納しておく。」
   - (3) 正：「3以上の自然数2k+1**が素数か否かをisPrime[k]で表すようにする**。」
   - 冒頭：「…奇数だけを**考慮すればよい**。**この性質を利用して、関数prime2に次の変更を加えた関数prime3を考える。**」

## B. 中程度

1. 図2のプログラム：`t ← [ウ]` の行が(L2)（MDの矢印位置は正しいが、行88のコメント位置を要確認）。冒頭宣言の型表記は「整数型の配列」「論理型の配列」（MD「整数型配列」）。
2. 図3のコメント：「isPrime[**k**]がtrueであれば2k+1が素数であることを表し…」（MD「isPrime[s]」）。(L3)は「isPrime[[オ]] ← false」の行（MDの矢印位置は合っている）。
3. 行97の後の文 正：「…関数prime2の(L2)の行の実行回数は、関数prime1の(L1)の行の実行回数**以下**となる。」（MD「半分以下」は誤り。「半分以下」はprime3とprime2の比較〔行144〕にのみ登場）

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 ア | N² | N² | ○ |
| 設問2 イ | isPrime[d]がtrueと等しい | isPrime[d-1]がtrueと等しい | **×要修正** |
| 設問2 ウ | t＋d | t+d | ○ |
| 設問3 エ | isPrime[(d－1)÷2]がtrueと等しい | 同一 | ○ |
| 設問3 オ | (t－1)÷2 | 同一 | ○ |
| 設問3 カ | t＋2×d | 同一 | ○ |
| 設問4 (L1) | 171 | 171（ただしN=28と誤記） | **△（N=20に修正）** |
| 設問4 (L2) | 13 | 13（同上） | **△** |
| 設問4 (L3) | 2 | 「13の半分以下」と曖昧 | **×要修正（=2）** |

## D. その他・画像確認

- 画像参照 `../images/2024秋_問03_図01_prime1プログラム.png`、`図02_prime2プログラム.png`、`図03_prime3プログラム.png` の実在と内容を修正時に確認（特に図1は正しいコードの画像か要確認）。
- 「力」（カタカナのカが漢字の力になっている箇所）を全て修正。
