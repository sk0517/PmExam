# 修正メモ：2025秋_問05_ネットワーク.md

照合元：`pdfs/2025r07a_ap_pm_qs.pdf` p.30〜34（問5本体）、公式解答 `pdfs/2025r07a_ap_pm_ans.pdf`
※解答は全問公式と一致。ただし運用形態の箇条書き2項目・移行後設定のNAT説明などが欠落し、表1の行も1行欠落。メール中継の方向も誤り。

## A. 重大（解答の根拠・設問に影響）

1. **表1の行欠落（行54〜60）— SMTP行が消えている**
   - 正しい表1は「インターネット→DMZ」が**3行**：
     | アクセス経路 | 送信元 | 宛先 | プロトコル／宛先ポート番号 |
     |---|---|---|---|
     | インターネット→DMZ | any | `[a]` | TCP/443 |
     | 〃 | any | `[b]` | TCP/53, UDP/53 |
     | 〃 | any | **メール中継サーバ** | **TCP/25** |
     | DMZ→インターネット | `[c]` | any | TCP/80, TCP/443 |
     | 〃 | キャッシュDNSサーバ | any | TCP/53, UDP/53 |
     | 〃 | メール中継サーバ | any | TCP/25 |
   - 「any→メール中継サーバ TCP/25」は設問2(1)（A社宛てSMTPパケット）の前提。

2. **運用形態の箇条書き2項目が欠落（行37〜46）— 設問2(3)の根拠**
   - 追加：「**業務サーバは、社内PCとだけ通信する。**」
   - 追加：「**社内PCは、内部DNSサーバで名前解決を行う。**」（設問2(3)で内部DNSサーバが解答に入る根拠）
   - また「業務サーバ**及びDMZの**Webサーバは、HTTP Over TLSによるアクセスだけを受け付ける。」（MDは「DMZの」欠落）

3. **メール中継サーバの説明の方向誤り（行41）**
   - 正：「メール中継サーバは、**メールサーバから送信された**社外宛てのメールを社外のメールサーバに中継し、社外のメールサーバからA社宛てに送信されたメールをメールサーバに中継する。」
   - MD「メールサーバ**へ**送信された」は誤り。設問1(2)の解答（社外→社外の中継を止める）と対になる正規中継の定義。

4. **下線②と設問1(2)の文言（行42・119）**
   - 本文 正：「・<u>②メール中継サーバでは、スパムメールの踏み台になることを避けるために、オープンリレー防止策を実施している</u>。」（MD末尾の「メール中継サーバが処理を止めるべき処理がある」は架空）
   - 設問1(2) 正：「本文中の下線②について、**メール中継サーバで防止すべき処理**を、40字以内で答えよ。」

5. **移行後設定のNAT説明の欠落（行96〜105）— 設問2(1)の根拠**
   - 追加：「**公開セグメントのサーバは、IGW経由でインターネットと通信する。IGWは、NATによって、公開セグメントの各サーバに設定された172.20.1.11〜172.20.1.14をインターネットに公開する各サーバの対応するIPアドレスである200.α.β.1〜200.α.β.3、200.α.β.5に変換する。**」
   - 追加：「**公開セグメントのインターネット向けドメイン名は、図1中のDMZと同様のA社.jpとし、A社.jpの各サーバには、図1と同じIPアドレスを設定する。**」
   - 追加：「**A社.jpのゾーン情報はインターネット向けDNSサービスで管理する。**」
   - 追加：「**本社は、IPsecルータ2及びIPsecルータ1を経由してBCL-Aと接続する。**」
   - MD行102「公開セグメントのサーバのインターネット向けのIPアドレスは、図1と同じグローバルIPアドレス（200.α.β.x）を設定する」は上記に置き換え。

6. **下線①の範囲と設定項目の欠落（行39）**
   - 正：「本社及び営業所のPC（以下、社内PCという）には、**L3SW1上で稼働する**DHCPサーバから**配付される**IPアドレス、**そのリース期間及びサブネットマスク**、<u>①**デフォルトゲートウェイのIPアドレス**</u>**並びに**DNSサーバのIPアドレスを設定している。」
   - 下線①は「デフォルトゲートウェイのIPアドレス」のみ（MDは「〜をもつ機器」まで下線）。

## B. 中程度（本文の誤変換）

1. 図1注記1 正：「**200.α.β.γは**グローバルIPアドレスである。また、200.α.β.0/28は**ネットワークアドレスを示し、/28はサブネットマスク長を示す**。」
2. 行70：正「社内PCを使用した業務の運用方法及び**PCの操作**は変更しない。」（MD「操作法」）
3. 行72：正「BCL-Aに移行後の**各サーバの**インターネット向けのIPアドレスには…」
4. 行96：正「移行した後の各種設定**、動作及び構成**を次に示す。」（MD「設定。動作」句点誤り）
5. 行98：正「公開セグメントと内部セグメントを**設定**する。」
6. 行99：正「経路表には、**サーバ同士の間及びサーバとインターネットとの間で移行前と同様な通信を行うことができる経路情報**を登録する。」（MD「セグメント同士の通信とインターネットへの通信」は誤り）
7. 行100：正「BCL-Aのドメイン名は、**A社内向けドメインのA社.lanとし**、ネットワークアドレスは172.20.0.0/20を設定する。この172.20.0.0/20をサブネットに分割して、**公開セグメントに172.20.1.0/24を、内部セグメントに172.20.2.0/24を設定する**。」
8. 行101：正「A社.lanのゾーン情報**は内部DNSサーバ**で管理する。」（MD「ゾーン情報内部向けDNSサーバ」）
9. 設問2(2)の設問文 正：「…BCL-Aのサーバに転送するパケットの**送信元のネットワークアドレス**を、図2中の表記で**全て**答えよ。」
10. 設問1(1)・(3)の設問文：「図1**中の**名称で答えよ。」

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1(1) | L3SW0 | L3SW0 | ○ |
| 設問1(2) | 社外のメールサーバから送信された社外宛てのメールを中継する処理 | 同一 | ○（本文の中継方向をA-3で修正） |
| 設問1(3) a | Webサーバ | Webサーバ | ○ |
| 設問1(3) b | 外部DNSサーバ | 外部DNSサーバ | ○ |
| 設問1(3) c | プロキシサーバ | プロキシサーバ | ○ |
| 設問2(1) | 通過前 200.α.β.2／通過後 172.20.1.14 | 同一 | ○（NAT根拠をA-5で復元） |
| 設問2(2) | 192.168.1.0/24、172.16.128.0/20 | 同一 | ○ |
| 設問2(3) | プロキシサーバ、メールサーバ、内部DNSサーバ | 同一 | ○（根拠2項目をA-2で復元） |

## D. その他・画像確認

- 画像参照 `../images/2025秋_問05_図01_現行ネットワーク構成.png`、`表01_FWルール.png`、`図02_BCL-A移行後ネットワーク.png` の実在と内容を修正時に確認。
- 図2の注記も転記推奨：「IGW：NAT機能をもつインターネットゲートウェイ」「注記 図1中の外部DNSサーバの機能は、B社が提供するインターネット向けDNSサービスを利用して実現する。」（後者は設問2(1)・図2理解の補助）
- 図2の構成：BCL-A（A社.lan 172.20.0.0/20）内に公開セグメント172.20.1.0/24・内部セグメント172.20.2.0/24・インターネット向けDNSサービス・IGW・IPsecルータ1。本社はIPsecルータ2─L3SW1。
- キーワード表は妥当。
