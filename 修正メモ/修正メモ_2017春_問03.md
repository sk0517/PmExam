# 修正メモ：2017春_問03_プログラミング.md

照合元：`pdfs/2017h29h_ap_pm_qs.pdf` p.14〜18（問3本体、図1〜図3・表1〜表3含む）、公式解答 `pdfs/2017h29h_ap_pm_ans.pdf`
※**解答はすべて公式と一致**（設問1 ア=isEmpty( )が0である/イ=ansStatus ← currentStatus/ウ=ansStatus ← currentStatus/エ=currentStatus.nextIndexが0ではない、設問2 オ="N"/カ="Y"、設問3(1)(A)→(B)→(C)→(D)→(E)→(F)→(G)・(2)(A)→(C)→(G)→(F)→(B)→(E)→(D)、設問4 ア、設問5 内容=数を降順にソートしておく。/理由=早い段階で探索を打ち切ることができる。）。**本文・図1〜図3・表1〜表3も正確で良好**。

## A. 重大（設問・解答に影響）

**設問4の解答表記に相違あり（IPA公式＝ア「2ⁿm」、MD＝エ「n²m」）**。MD解答部（行196〜200）は「エ（n²m）」としているが、IPA公式解答は**ア（2ⁿm）**。幅優先探索では最下層に最大2ⁿ個の状態がキューに存在し得るため、メモリ使用量の最大値は2ⁿm（記号ア）が正しい。MDの記号エ・解説（n²mのオーダー）は誤り。※MDの解答群自体（ア=2ⁿm…オ=(n+1)m）はPDFと一致しており、選んだ記号と数式のみ誤り。

## B. 中程度（本文・字句）

特になし。〔問題〕定義（n個の自然数から目標Xに最も近い組合せ・例(10,34,55,77)→99）、図1場合分け木構造（初期状態A〜状態G）、〔データ構造の検討〕構造体Status・表1（numbers/target/Status〈total/selectedNumbers/nextIndex〉/currentStatus/ansStatus）、〔探索の手順〕下線①キュー/スタック・下線②メモリ使用量・(1)〜(3)、〔"取得した状態"の評価〕(1)(2)・表2関数（enqueue/dequeue/isEmpty/nextStatus1/nextStatus2/abs）、〔探索処理関数treeSearch〕図2（空欄ア〜エ・(α)(β)）、〔探索回数の削減〕下線③・(1)〜(3)・表3 nextFlag・図3（空欄オ/カ）すべてPDFとMDが一致。

## C. 公式解答一覧（照合済み）

| 設問 | 公式解答 | MDの記載 | 判定 |
|---|---|---|---|
| 設問1 ア | isEmpty( )が0である | 同一 | ○ |
| 設問1 イ | ansStatus ← currentStatus | 同一 | ○ |
| 設問1 ウ | ansStatus ← currentStatus | 同一 | ○ |
| 設問1 エ | currentStatus.nextIndexが0ではない | 同一 | ○ |
| 設問2 オ | "N" | "N" | ○ |
| 設問2 カ | "Y" | "Y" | ○ |
| 設問3(1) | (A)→(B)→(C)→(D)→(E)→(F)→(G) | 同一 | ○ |
| 設問3(2) | (A)→(C)→(G)→(F)→(B)→(E)→(D) | 同一 | ○ |
| 設問4 | ア（2ⁿm） | エ（n²m） | ✗ |
| 設問5 内容 | 数を降順にソートしておく。 | 同一 | ○ |
| 設問5 理由 | 早い段階で探索を打ち切ることができる。 | 同一 | ○ |

## D. その他・画像確認

- 画像参照 `../images/2017春_問03_図01_場合分け.png` の実在と内容を修正時に確認。図2/図3擬似コード・表1〜表3はMD内にテキスト転記済みでPDF一致（空欄ア〜カ配置もPDF通り）。
- **修正必要箇所**：設問4のMD解答を「エ（n²m）」→「**ア（2ⁿm）**」に訂正し、解説（行198）も幅優先探索の最下層に最大2ⁿ個の状態がキューに存在し得る旨に書き換える。
- 他の設問論理（設問1=キュー空でない/ansStatus更新/nextIndex≠0、設問2=目標X以上でN、設問3=BFS/DFSの評価順、設問5=降順ソートで早期打切り）はMD解説が正確。
- キーワード表（行214〜222）は妥当（木構造探索、キュー/BFS、スタック/DFS、枝刈り、メモリ使用量オーダー）。
