# 2024年春期（令和6年度春期）応用情報技術者試験 午後 問7（選択）
## 組込みシステム：業務用ホットコーヒーマシンの制御（リアルタイムOS・状態遷移）

---

## 問題文

**問7** 業務用ホットコーヒーマシンに関する次の記述を読んで、設問に答えよ。

G社は、業務用ホットコーヒーマシン（以下、コーヒーマシンという）を開発している。コーヒーマシンの外観を図1に、コーヒーマシンの内部構成を図2に、コーヒーマシンの主な構成要素を表1に、それぞれ示す。

### 図1・図2 コーヒーマシンの外観と内部構成

![図1・図2 外観と内部構成](images/2024春_問07_図01_外観と内部構成.png)

> **外観の主な構成：**
> - タッチパネル（画面操作）
> - 抽出部（コーヒー注ぎ出し口）
> - ドア（コーヒー注ぎ出し口の下に位置）
> - カメラ
> - カップ載置部
>
> **内部構成：**
> - 制御部 → タッチパネル / 抽出部 / ドア / カメラ / カップ載置部

> | 構成要素名 | 概要 |
> |---|---|
> | 制御部 | コーヒーマシン全体の制御及び状態管理等を行う。カップの有無やサイズ、カップが砕かれるよう通知を処理する |
> | タッチパネル | 制御部から指示された画面をタッチパネルに表示する |
> | 抽出部 | 制御部から指示されたサイズのコーヒーを抽出し、コーヒー排出口から排出する |
> | ドア | 開閉センサーをもち、閉状態は1と開状態は0のデジタル信号で制御部に入力する。ドアをロックした状態でロックすることができ、ロック機構をもつ。ロック機構は制御部からのロック及びロック解除の信号を受け付け、ドアのロック及びロック解除の動作をする |
> | カメラ | コーヒー排出口から排出されたコーヒーを受けるカップを撮影する場所 |
> | カップ載置部 | 制御部から制御されるカメラで、制御部からの指示を受け取り、制御部に判定に使用するデータを書き込む |

---

### 〔カップ判定の概要〕

カップ判定は、利用者がドアを開いた時に、カメラでカップ載置部を複数回撮影して行う。カップ判定の結果一覧を表2に示す。

### 表2 カップ判定の結果一覧

![表2 カップ判定結果](images/2024春_問07_表02_カップ判定結果.png)

> | 結果 | 概要 |
> |---|---|
> | カップなし | カップ載置部に何も置かれていないことを示す |
> | 空カップあり | 専用のカップが空の状態でカップ載置部に置かれていることを示す。この結果には、カップのサイズ（大、中、小）が付加される |
> | カップあり | 専用のカップが定まった状態でカップ載置部に置かれていることを示す |
> | 障害物あり | 専用のカップ以外のものがカップ載置部に置かれていることを示す |

---

### 〔ドアの開閉状態の判定仕様〕

ドアの開閉センサーは、ドアが完全に閉じているときは8、それ以外は1を出力する。入力に短い間隔で0と1とを交互に出力することがあるため、制御部のソフトウェアは入力された値を10ミリ秒間隔で読み出し、4回連続で同じ値が読み出されたらドアの開閉状態を確定するものとする。

---

### 〔コーヒーマシンの動作概要〕

コーヒーマシンの動作概要を次に示す。
1. 電源が入ると、初期化処理を行う。初期化処理が完了した後には特機中となり、カップ載置部には何もないように促す画面をタッチパネルに通知する。
2. 利用者がドアを開けて、購入したカップをカップ載置部に置く。
3. 利用者がドアを閉めて、カップ判定が始まる。
4. カップ判定の結果が「空カップあり」となるなどで、カップのサイズを示す文字と、確認ボタンで確認を促す画面をタッチパネルに表示する。
5. 利用者が確認ボタンにタッチすると、`[　a　]`、カップのサイズに応じたコーヒーを抽出するコーヒー注ぎ出しを開始し、タッチパネルには、抽出中であることを示す画面を表示する。
6. コーヒーの排出が終わる。タッチパネルにカップの引取りを促す画面を表示する。
7. 利用者がカップを引き取る。
8. 利用者がドアを閉める。カップ判定が行われる。
9. カップ判定の結果が「カップなし」となって、待機中に戻る。

ここで、カップ判定中に利用者がドアを開けた場合は、カップ判定を中止し、利用者がドアを閉じるのを待つ。また、確認ボタンがタッチされる前に、利用者がドアを開けた場合は、カップ判定の結果を破棄し、利用者がドアを閉じるのを待つ。カップ判定の結果が「カップあり」又は「障害物あり」の場合、カップ判定の結果に応じた適切な画面をタッチパネルに表示する。

---

### 〔制御部のソフトウェア構成〕

制御部のソフトウェアは、リアルタイムOSを用いて実装する。制御部の主なタスクの処理概要を表3に示す。

### 表3 制御部の主なタスクの処理概要

![表3 タスク処理概要](images/2024春_問07_表03_タスク処理概要.png)

> | タスク名 | 処理概要 |
> |---|---|
> | メイン | コーヒーマシン全体の状態管理を行う。メインタスクからカメラやドアへの指示、タッチパネル、カップ判定、ドアの制御を行う「カップ判定」に通知する。ドアが開いたとき "ドア開" を、ドアが閉じたとき "ドア閉" を メインタスクに通知する。カップ判定が終わると "判定結果" を メインタスクに通知する |
> | カップ判定 | メインタスクから "判定" を受けると、カメラを複数回起動し、カップの判定を行い、終わったらカップ判定の結果を "判定結果" としてメインタスクに通知する。サブタスクからの指示によって "抽出中止" の指示に随時応じる |
> | 抽出 | メインタスクから "抽出" を受けると、抽出部を起動してコーヒーを抽出し終わったら "抽出完了" をメインタスクに通知する |
> | タッチパネル | メインタスクから "画面表示" を受けると、指示された画面をタッチパネルに表示する。確認ボタンを検出し "確認" をメインタスクに通知する |
> | ドア | 開閉センサーの出力を 10ミリ秒ごとに読み出し、確定したドアの開閉状態を保持する。メインタスクから "ロック" 及び "ロック解除" を受けてロックとロック解除の制御を行う |

---

### 図3 割込みセンサーの出力とドアタスクの動作タイミングの例

![図3 タイミング図](images/2024春_問07_図03_タイミング図.png)

> - 実線は開閉センサーの出力変化を示し、破線はドアタスクの動作タイミングを示す
> - アの時点ではドアタスクが保持しているドアの開閉状態が閉状態
> - 4回連続で同じ値が読み出されたらドアの開閉状態を確定

---

### 図4 メインタスクの状態遷移

![図4 状態遷移図](images/2024春_問07_図04_状態遷移図.png)

> **状態一覧：**
> - 待機中 → （ドア開）→ カップ判定 → （判定結果：空カップあり）→ 確認待ち → （確認）→ 抽出中 → （抽出完了）→ 引き取り待ち
> - カップ判定 → （ドア開）→ 中止待ち → （ドア閉）→ カップ判定
> - 確認待ち → （ドア開）→ 中止待ち
> - 各状態から"中止完了"又は `[　b　]` → 待機中へ
> - 引き取り待ち → ドア閉 → カップ判定 → カップなし → 待機中

---

## 設問

### 設問1 コーヒーマシンについて答えよ。

**(1)** 本文中の `[　a　]` に入れる、適切なコーヒーマシンの動作を答えよ。

**(2)** 開閉センサーの出力を読み出す周期を、周波数32kHzのカウントダウンタイマー（以下、タイマーという）を用いて計る。このタイマーは、あらかじめ設定された初期値からカウントダウンを行い、カウント値が0になったとき、次のカウントダウンを行う際の初期期を自動的にリロードして継続する。タイマーに設定する初期値を、整数で求めよ。（1k=10³とする）

### 設問2 制御部のタスクについて答えよ。

**(1)** カップ判定タスクは、メインタスク及びドアタスクよりも優先度を低くしている。その理由を30字以内で答えよ。

**(2)** メインタスクが抽出タスクに「抽出」を通知する際の必要な情報パラメータとして、必要な情報を答えよ。

**(3)** 開閉センサーの出力と、ドアタスクの動作タイミングの例を図3に示す。図3中の、アの時点でドアタスクが保持しているドアの開閉状態が開状態であるとき、ドアタスクがメインタスクに「ドア開」及び「ドア閉」を通知するタイミングを、それぞれアーケの記号で答えよ。

### 設問3 図4に示すメインタスクの状態遷移について答えよ。

**(1)** メインタスクがドアタスクに通知を行うのは、何のメッセージを受けたときか。図4中のメッセージ名で全て答えよ。

**(2)** 図4中の `[　b　]` に入れる適切なメッセージ名を、表3中の字句で答えよ。

---

## 解答と解説

### 設問1

**(1) 正解：a=ドアをロック**

確認ボタンを押した後、コーヒー抽出中にドアが開かないようにするため、まず「ドアをロック」する。その後コーヒーを抽出し始める。

**(2) 正解：320**

- 読み出し周期: 10ミリ秒 = 10×10⁻³秒
- タイマー周波数: 32kHz = 32×10³Hz
- 初期値 = 周波数 × 周期 = 32,000 × 0.010 = **320**

---

### 設問2

**(1) 正解：カップ判定中にドアが開けられたことを検出したいから（25字）**

カップ判定中（カメラ撮影中）にドアが開かれた場合は、即座にカップ判定を中止する必要がある。ドアタスクが高優先度であれば、ドア状態の変化をリアルタイムに検知できる。

**(2) 正解：コーヒーの分量（カップのサイズに対応した量）**

抽出タスクがコーヒーを正確な量で抽出するためには、カップのサイズ（大・中・小）に対応した分量情報が必要。

**(3) 正解：ドア開=タ、ドア閉=カ**

図3のタイミング図に従い、4回連続で同じ値が読み出された時点でドア状態が確定する。

---

### 設問3

**(1) 正解：「確認」、「抽出完了」**

- **「確認」**を受けたとき → ドアタスクに「ロック」を通知
- **「抽出完了」**を受けたとき → ドアタスクに「ロック解除」を通知

**(2) 正解：b=判定結果**

図4の状態遷移図で、中止待ちや確認待ちなどから待機中に戻るトリガーとして「中止完了」以外に必要なメッセージは「判定結果」（カップなしの結果を受けて待機中に戻る）。

---

## 参考：主要キーワード

| 用語 | 説明 |
|------|------|
| リアルタイムOS（RTOS） | タスクの優先度に基づいてリアルタイムに処理するOS。組込みシステムで広く使用 |
| タスク | RTOSにおける処理単位。優先度が設定され、高優先度タスクが先に実行される |
| タスク間通信 | メッセージパッシングやキューを使ってタスク間でデータを受け渡す仕組み |
| カウントダウンタイマー | 設定値からカウントダウンし、0になると割込みを発生させるタイマー |
| チャタリング | スイッチやセンサーの接点が切り替わるとき、短時間に0と1が繰り返す現象 |
| 状態遷移 | システムの状態が特定のイベントをトリガーとして別の状態に変化すること |
| 割込み | ハードウェア（センサー等）からのイベントでCPUに即時処理を要求する仕組み |
| ロック/ロック解除 | 物理的なドア施錠機構への制御コマンド |
| 優先度制御 | RTOSで優先度の高いタスクが低いタスクを割込んで実行する仕組み（プリエンプション） |
